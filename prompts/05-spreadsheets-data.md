# 📊 Spreadsheets & Data Wrangling — 90 copyable GPT-6 Astra prompts

> Excel/Sheets models, cleaning recipes, and reconciliation flows.

> Safety: defensive / authorized-scope use only. Stop before destructive, irreversible, sending, purchasing, deleting, or permission-changing steps without approval.

**Jump:** [⬆️ Index](../README.md#-category-carousel) · [◀ Prev](04-professional-docs.md) · [Next ▶](06-science-research.md) · [🔍 Search all](prompts_index.json) · [🖱️ Interactive carousel](../docs/carousel.html)

Copy any prompt: click the copy icon on its code block.

### `spreadsheets-data-001` — reconcile two transaction exports and isolate mismatches

```text
Act as a senior data analyst. Reconcile two transaction exports and isolate mismatches for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `spreadsheets-data-002` — create a hiring pipeline funnel with conversion rates

```text
Act as an expert spreadsheet modeler. Create a hiring pipeline funnel with conversion rates for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `spreadsheets-data-003` — design a data-entry template with validation rules

```text
Act as a senior FP&A analyst. Design a data-entry template with validation rules for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `spreadsheets-data-004` — build an inventory reorder sheet with lead-time math

```text
Act as an operations data lead. Build an inventory reorder sheet with lead-time math for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `spreadsheets-data-005` — build a marketing attribution sheet with UTM hygiene checks

```text
Act as a senior RevOps analyst. Build a marketing attribution sheet with UTM hygiene checks for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `spreadsheets-data-006` — design a commission model with caps and accelerators

```text
Act as an expert data-quality engineer. Design a commission model with caps and accelerators for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `spreadsheets-data-007` — create a loan amortization schedule with extra-payment scenarios

```text
Act as a senior data analyst. Create a loan amortization schedule with extra-payment scenarios for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `spreadsheets-data-008` — create a cohort retention table from raw events

```text
Act as an expert spreadsheet modeler. Create a cohort retention table from raw events for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `spreadsheets-data-009` — build a capacity plan from velocity and leave data

```text
Act as a senior FP&A analyst. Build a capacity plan from velocity and leave data for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `spreadsheets-data-010` — build a budget tracker with variance alerts

```text
Act as an operations data lead. Build a budget tracker with variance alerts for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `spreadsheets-data-011` — reconcile invoices against POs and flag exceptions

```text
Act as a senior RevOps analyst. Reconcile invoices against POs and flag exceptions for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `spreadsheets-data-012` — clean a messy contact list: dedupe, normalize, validate

```text
Act as an expert data-quality engineer. Clean a messy contact list: dedupe, normalize, validate for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `spreadsheets-data-013` — design a survey analysis sheet with significance flags

```text
Act as a senior data analyst. Design a survey analysis sheet with significance flags for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `spreadsheets-data-014` — build a pricing calculator with tiers, discounts, and guardrails

```text
Act as an expert spreadsheet modeler. Build a pricing calculator with tiers, discounts, and guardrails for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `spreadsheets-data-015` — build a churn-risk score from usage signals

```text
Act as a senior FP&A analyst. Build a churn-risk score from usage signals for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `spreadsheets-data-016` — reconcile two transaction exports and isolate mismatches

```text
Act as an operations data lead. Reconcile two transaction exports and isolate mismatches for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `spreadsheets-data-017` — create a hiring pipeline funnel with conversion rates

```text
Act as a senior RevOps analyst. Create a hiring pipeline funnel with conversion rates for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `spreadsheets-data-018` — design a data-entry template with validation rules

```text
Act as an expert data-quality engineer. Design a data-entry template with validation rules for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `spreadsheets-data-019` — build an inventory reorder sheet with lead-time math

```text
Act as a senior data analyst. Build an inventory reorder sheet with lead-time math for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `spreadsheets-data-020` — build a marketing attribution sheet with UTM hygiene checks

```text
Act as an expert spreadsheet modeler. Build a marketing attribution sheet with UTM hygiene checks for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `spreadsheets-data-021` — design a commission model with caps and accelerators

```text
Act as a senior FP&A analyst. Design a commission model with caps and accelerators for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `spreadsheets-data-022` — create a loan amortization schedule with extra-payment scenarios

```text
Act as an operations data lead. Create a loan amortization schedule with extra-payment scenarios for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `spreadsheets-data-023` — create a cohort retention table from raw events

```text
Act as a senior RevOps analyst. Create a cohort retention table from raw events for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `spreadsheets-data-024` — build a capacity plan from velocity and leave data

```text
Act as an expert data-quality engineer. Build a capacity plan from velocity and leave data for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `spreadsheets-data-025` — build a budget tracker with variance alerts

```text
Act as a senior data analyst. Build a budget tracker with variance alerts for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `spreadsheets-data-026` — reconcile invoices against POs and flag exceptions

```text
Act as an expert spreadsheet modeler. Reconcile invoices against POs and flag exceptions for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `spreadsheets-data-027` — clean a messy contact list: dedupe, normalize, validate

```text
Act as a senior FP&A analyst. Clean a messy contact list: dedupe, normalize, validate for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `spreadsheets-data-028` — design a survey analysis sheet with significance flags

```text
Act as an operations data lead. Design a survey analysis sheet with significance flags for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `spreadsheets-data-029` — build a pricing calculator with tiers, discounts, and guardrails

```text
Act as a senior RevOps analyst. Build a pricing calculator with tiers, discounts, and guardrails for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `spreadsheets-data-030` — build a churn-risk score from usage signals

```text
Act as an expert data-quality engineer. Build a churn-risk score from usage signals for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `spreadsheets-data-031` — reconcile two transaction exports and isolate mismatches

```text
Act as a senior data analyst. Reconcile two transaction exports and isolate mismatches for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 31.
```

### `spreadsheets-data-032` — create a hiring pipeline funnel with conversion rates

```text
Act as an expert spreadsheet modeler. Create a hiring pipeline funnel with conversion rates for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 32.
```

### `spreadsheets-data-033` — design a data-entry template with validation rules

```text
Act as a senior FP&A analyst. Design a data-entry template with validation rules for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 33.
```

### `spreadsheets-data-034` — build an inventory reorder sheet with lead-time math

```text
Act as an operations data lead. Build an inventory reorder sheet with lead-time math for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 34.
```

### `spreadsheets-data-035` — build a marketing attribution sheet with UTM hygiene checks

```text
Act as a senior RevOps analyst. Build a marketing attribution sheet with UTM hygiene checks for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 35.
```

### `spreadsheets-data-036` — design a commission model with caps and accelerators

```text
Act as an expert data-quality engineer. Design a commission model with caps and accelerators for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 36.
```

### `spreadsheets-data-037` — create a loan amortization schedule with extra-payment scenarios

```text
Act as a senior data analyst. Create a loan amortization schedule with extra-payment scenarios for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 37.
```

### `spreadsheets-data-038` — create a cohort retention table from raw events

```text
Act as an expert spreadsheet modeler. Create a cohort retention table from raw events for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 38.
```

### `spreadsheets-data-039` — build a capacity plan from velocity and leave data

```text
Act as a senior FP&A analyst. Build a capacity plan from velocity and leave data for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 39.
```

### `spreadsheets-data-040` — build a budget tracker with variance alerts

```text
Act as an operations data lead. Build a budget tracker with variance alerts for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 40.
```

### `spreadsheets-data-041` — reconcile invoices against POs and flag exceptions

```text
Act as a senior RevOps analyst. Reconcile invoices against POs and flag exceptions for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 41.
```

### `spreadsheets-data-042` — clean a messy contact list: dedupe, normalize, validate

```text
Act as an expert data-quality engineer. Clean a messy contact list: dedupe, normalize, validate for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 42.
```

### `spreadsheets-data-043` — design a survey analysis sheet with significance flags

```text
Act as a senior data analyst. Design a survey analysis sheet with significance flags for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 43.
```

### `spreadsheets-data-044` — build a pricing calculator with tiers, discounts, and guardrails

```text
Act as an expert spreadsheet modeler. Build a pricing calculator with tiers, discounts, and guardrails for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 44.
```

### `spreadsheets-data-045` — build a churn-risk score from usage signals

```text
Act as a senior FP&A analyst. Build a churn-risk score from usage signals for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 45.
```

### `spreadsheets-data-046` — reconcile two transaction exports and isolate mismatches

```text
Act as an operations data lead. Reconcile two transaction exports and isolate mismatches for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 46.
```

### `spreadsheets-data-047` — create a hiring pipeline funnel with conversion rates

```text
Act as a senior RevOps analyst. Create a hiring pipeline funnel with conversion rates for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 47.
```

### `spreadsheets-data-048` — design a data-entry template with validation rules

```text
Act as an expert data-quality engineer. Design a data-entry template with validation rules for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 48.
```

### `spreadsheets-data-049` — build an inventory reorder sheet with lead-time math

```text
Act as a senior data analyst. Build an inventory reorder sheet with lead-time math for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 49.
```

### `spreadsheets-data-050` — build a marketing attribution sheet with UTM hygiene checks

```text
Act as an expert spreadsheet modeler. Build a marketing attribution sheet with UTM hygiene checks for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 50.
```

### `spreadsheets-data-051` — design a commission model with caps and accelerators

```text
Act as a senior FP&A analyst. Design a commission model with caps and accelerators for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 51.
```

### `spreadsheets-data-052` — create a loan amortization schedule with extra-payment scenarios

```text
Act as an operations data lead. Create a loan amortization schedule with extra-payment scenarios for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 52.
```

### `spreadsheets-data-053` — create a cohort retention table from raw events

```text
Act as a senior RevOps analyst. Create a cohort retention table from raw events for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 53.
```

### `spreadsheets-data-054` — build a capacity plan from velocity and leave data

```text
Act as an expert data-quality engineer. Build a capacity plan from velocity and leave data for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 54.
```

### `spreadsheets-data-055` — build a budget tracker with variance alerts

```text
Act as a senior data analyst. Build a budget tracker with variance alerts for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 55.
```

### `spreadsheets-data-056` — reconcile invoices against POs and flag exceptions

```text
Act as an expert spreadsheet modeler. Reconcile invoices against POs and flag exceptions for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 56.
```

### `spreadsheets-data-057` — clean a messy contact list: dedupe, normalize, validate

```text
Act as a senior FP&A analyst. Clean a messy contact list: dedupe, normalize, validate for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 57.
```

### `spreadsheets-data-058` — design a survey analysis sheet with significance flags

```text
Act as an operations data lead. Design a survey analysis sheet with significance flags for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 58.
```

### `spreadsheets-data-059` — build a pricing calculator with tiers, discounts, and guardrails

```text
Act as a senior RevOps analyst. Build a pricing calculator with tiers, discounts, and guardrails for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 59.
```

### `spreadsheets-data-060` — build a churn-risk score from usage signals

```text
Act as an expert data-quality engineer. Build a churn-risk score from usage signals for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 60.
```

### `spreadsheets-data-061` — reconcile two transaction exports and isolate mismatches

```text
Act as a senior data analyst. Reconcile two transaction exports and isolate mismatches for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 61.
```

### `spreadsheets-data-062` — create a hiring pipeline funnel with conversion rates

```text
Act as an expert spreadsheet modeler. Create a hiring pipeline funnel with conversion rates for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 62.
```

### `spreadsheets-data-063` — design a data-entry template with validation rules

```text
Act as a senior FP&A analyst. Design a data-entry template with validation rules for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 63.
```

### `spreadsheets-data-064` — build an inventory reorder sheet with lead-time math

```text
Act as an operations data lead. Build an inventory reorder sheet with lead-time math for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 64.
```

### `spreadsheets-data-065` — build a marketing attribution sheet with UTM hygiene checks

```text
Act as a senior RevOps analyst. Build a marketing attribution sheet with UTM hygiene checks for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 65.
```

### `spreadsheets-data-066` — design a commission model with caps and accelerators

```text
Act as an expert data-quality engineer. Design a commission model with caps and accelerators for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 66.
```

### `spreadsheets-data-067` — create a loan amortization schedule with extra-payment scenarios

```text
Act as a senior data analyst. Create a loan amortization schedule with extra-payment scenarios for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 67.
```

### `spreadsheets-data-068` — create a cohort retention table from raw events

```text
Act as an expert spreadsheet modeler. Create a cohort retention table from raw events for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 68.
```

### `spreadsheets-data-069` — build a capacity plan from velocity and leave data

```text
Act as a senior FP&A analyst. Build a capacity plan from velocity and leave data for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 69.
```

### `spreadsheets-data-070` — build a budget tracker with variance alerts

```text
Act as an operations data lead. Build a budget tracker with variance alerts for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 70.
```

### `spreadsheets-data-071` — reconcile invoices against POs and flag exceptions

```text
Act as a senior RevOps analyst. Reconcile invoices against POs and flag exceptions for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 71.
```

### `spreadsheets-data-072` — clean a messy contact list: dedupe, normalize, validate

```text
Act as an expert data-quality engineer. Clean a messy contact list: dedupe, normalize, validate for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 72.
```

### `spreadsheets-data-073` — design a survey analysis sheet with significance flags

```text
Act as a senior data analyst. Design a survey analysis sheet with significance flags for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 73.
```

### `spreadsheets-data-074` — build a pricing calculator with tiers, discounts, and guardrails

```text
Act as an expert spreadsheet modeler. Build a pricing calculator with tiers, discounts, and guardrails for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 74.
```

### `spreadsheets-data-075` — build a churn-risk score from usage signals

```text
Act as a senior FP&A analyst. Build a churn-risk score from usage signals for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 75.
```

### `spreadsheets-data-076` — reconcile two transaction exports and isolate mismatches

```text
Act as an operations data lead. Reconcile two transaction exports and isolate mismatches for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 76.
```

### `spreadsheets-data-077` — create a hiring pipeline funnel with conversion rates

```text
Act as a senior RevOps analyst. Create a hiring pipeline funnel with conversion rates for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 77.
```

### `spreadsheets-data-078` — design a data-entry template with validation rules

```text
Act as an expert data-quality engineer. Design a data-entry template with validation rules for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 78.
```

### `spreadsheets-data-079` — build an inventory reorder sheet with lead-time math

```text
Act as a senior data analyst. Build an inventory reorder sheet with lead-time math for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 79.
```

### `spreadsheets-data-080` — build a marketing attribution sheet with UTM hygiene checks

```text
Act as an expert spreadsheet modeler. Build a marketing attribution sheet with UTM hygiene checks for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 80.
```

### `spreadsheets-data-081` — design a commission model with caps and accelerators

```text
Act as a senior FP&A analyst. Design a commission model with caps and accelerators for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 81.
```

### `spreadsheets-data-082` — create a loan amortization schedule with extra-payment scenarios

```text
Act as an operations data lead. Create a loan amortization schedule with extra-payment scenarios for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 82.
```

### `spreadsheets-data-083` — create a cohort retention table from raw events

```text
Act as a senior RevOps analyst. Create a cohort retention table from raw events for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 83.
```

### `spreadsheets-data-084` — build a capacity plan from velocity and leave data

```text
Act as an expert data-quality engineer. Build a capacity plan from velocity and leave data for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 84.
```

### `spreadsheets-data-085` — build a budget tracker with variance alerts

```text
Act as a senior data analyst. Build a budget tracker with variance alerts for a finance team. The intended result is a clean workbook spec plus formulas. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 85.
```

### `spreadsheets-data-086` — reconcile invoices against POs and flag exceptions

```text
Act as an expert spreadsheet modeler. Reconcile invoices against POs and flag exceptions for a manufacturing planner. The intended result is a QA checklist with check totals. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 86.
```

### `spreadsheets-data-087` — clean a messy contact list: dedupe, normalize, validate

```text
Act as a senior FP&A analyst. Clean a messy contact list: dedupe, normalize, validate for a SaaS startup. The intended result is a model with sensitivity scenarios. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 87.
```

### `spreadsheets-data-088` — design a survey analysis sheet with significance flags

```text
Act as an operations data lead. Design a survey analysis sheet with significance flags for a marketplace seller. The intended result is a CSV of cleaned rows plus a reject log. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 88.
```

### `spreadsheets-data-089` — build a pricing calculator with tiers, discounts, and guardrails

```text
Act as a senior RevOps analyst. Build a pricing calculator with tiers, discounts, and guardrails for a nonprofit. The intended result is a dashboard-ready summary with charts described. Requirements: preserve raw data on a separate untouched sheet; every formula must be auditable; no hard-coded magic numbers. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 89.
```

### `spreadsheets-data-090` — build a churn-risk score from usage signals

```text
Act as an expert data-quality engineer. Build a churn-risk score from usage signals for a sales ops team. The intended result is a reconciliation table with exception reasons. Requirements: validate totals against source data before concluding; use consistent date, currency, and timezone handling. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 90.
```
