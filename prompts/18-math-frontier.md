# 🧮 Math (Frontier-Style Rigor) — 90 copyable GPT-6 Astra prompts

> Proof sketches, counterexamples, and verification discipline.

> Safety: defensive / authorized-scope use only. Stop before destructive, irreversible, sending, purchasing, deleting, or permission-changing steps without approval.

**Jump:** [⬆️ Index](../README.md#-category-carousel) · [◀ Prev](17-education.md) · [Next ▶](19-data-viz.md) · [🔍 Search all](prompts_index.json) · [🖱️ Interactive carousel](../docs/carousel.html)

Copy any prompt: click the copy icon on its code block.

### `math-frontier-001` — prove a combinatorial identity with a clear lemma chain

```text
Act as a senior mathematician aide. Prove a combinatorial identity with a clear lemma chain for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `math-frontier-002` — prove an inequality with named theorems

```text
Act as an expert contest coach. Prove an inequality with named theorems for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `math-frontier-003` — write a solution sketch plus a full-solution expansion

```text
Act as a patient proof tutor. Write a solution sketch plus a full-solution expansion for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `math-frontier-004` — analyze a recurrence with generating functions

```text
Act as a senior statistics mentor. Analyze a recurrence with generating functions for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `math-frontier-005` — design a practice set that ramps difficulty

```text
Act as an expert discrete-math guide. Design a practice set that ramps difficulty for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `math-frontier-006` — design a counting argument with bijection

```text
Act as a senior olympiad trainer. Design a counting argument with bijection for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `math-frontier-007` — explain Bayes with a worked medical-test example

```text
Act as a senior mathematician aide. Explain Bayes with a worked medical-test example for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `math-frontier-008` — explain a FrontierMath-style problem's structure without solving

```text
Act as an expert contest coach. Explain a FrontierMath-style problem's structure without solving for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `math-frontier-009` — build a probability model with assumptions stated

```text
Act as a patient proof tutor. Build a probability model with assumptions stated for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `math-frontier-010` — build intuition for eigenvalues with worked cases

```text
Act as a senior statistics mentor. Build intuition for eigenvalues with worked cases for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `math-frontier-011` — translate a word problem into equations cleanly

```text
Act as an expert discrete-math guide. Translate a word problem into equations cleanly for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `math-frontier-012` — solve a number-theory problem with modular reasoning

```text
Act as a senior olympiad trainer. Solve a number-theory problem with modular reasoning for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `math-frontier-013` — verify a proof step by step and flag gaps

```text
Act as a senior mathematician aide. Verify a proof step by step and flag gaps for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `math-frontier-014` — find a counterexample to a plausible conjecture

```text
Act as an expert contest coach. Find a counterexample to a plausible conjecture for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `math-frontier-015` — explore prime gaps computationally with a plan

```text
Act as a patient proof tutor. Explore prime gaps computationally with a plan for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `math-frontier-016` — prove a combinatorial identity with a clear lemma chain

```text
Act as a senior statistics mentor. Prove a combinatorial identity with a clear lemma chain for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `math-frontier-017` — prove an inequality with named theorems

```text
Act as an expert discrete-math guide. Prove an inequality with named theorems for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `math-frontier-018` — write a solution sketch plus a full-solution expansion

```text
Act as a senior olympiad trainer. Write a solution sketch plus a full-solution expansion for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `math-frontier-019` — analyze a recurrence with generating functions

```text
Act as a senior mathematician aide. Analyze a recurrence with generating functions for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `math-frontier-020` — design a practice set that ramps difficulty

```text
Act as an expert contest coach. Design a practice set that ramps difficulty for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `math-frontier-021` — design a counting argument with bijection

```text
Act as a patient proof tutor. Design a counting argument with bijection for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `math-frontier-022` — explain Bayes with a worked medical-test example

```text
Act as a senior statistics mentor. Explain Bayes with a worked medical-test example for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `math-frontier-023` — explain a FrontierMath-style problem's structure without solving

```text
Act as an expert discrete-math guide. Explain a FrontierMath-style problem's structure without solving for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `math-frontier-024` — build a probability model with assumptions stated

```text
Act as a senior olympiad trainer. Build a probability model with assumptions stated for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `math-frontier-025` — build intuition for eigenvalues with worked cases

```text
Act as a senior mathematician aide. Build intuition for eigenvalues with worked cases for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `math-frontier-026` — translate a word problem into equations cleanly

```text
Act as an expert contest coach. Translate a word problem into equations cleanly for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `math-frontier-027` — solve a number-theory problem with modular reasoning

```text
Act as a patient proof tutor. Solve a number-theory problem with modular reasoning for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `math-frontier-028` — verify a proof step by step and flag gaps

```text
Act as a senior statistics mentor. Verify a proof step by step and flag gaps for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `math-frontier-029` — find a counterexample to a plausible conjecture

```text
Act as an expert discrete-math guide. Find a counterexample to a plausible conjecture for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `math-frontier-030` — explore prime gaps computationally with a plan

```text
Act as a senior olympiad trainer. Explore prime gaps computationally with a plan for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `math-frontier-031` — prove a combinatorial identity with a clear lemma chain

```text
Act as a senior mathematician aide. Prove a combinatorial identity with a clear lemma chain for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 31.
```

### `math-frontier-032` — prove an inequality with named theorems

```text
Act as an expert contest coach. Prove an inequality with named theorems for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 32.
```

### `math-frontier-033` — write a solution sketch plus a full-solution expansion

```text
Act as a patient proof tutor. Write a solution sketch plus a full-solution expansion for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 33.
```

### `math-frontier-034` — analyze a recurrence with generating functions

```text
Act as a senior statistics mentor. Analyze a recurrence with generating functions for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 34.
```

### `math-frontier-035` — design a practice set that ramps difficulty

```text
Act as an expert discrete-math guide. Design a practice set that ramps difficulty for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 35.
```

### `math-frontier-036` — design a counting argument with bijection

```text
Act as a senior olympiad trainer. Design a counting argument with bijection for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 36.
```

### `math-frontier-037` — explain Bayes with a worked medical-test example

```text
Act as a senior mathematician aide. Explain Bayes with a worked medical-test example for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 37.
```

### `math-frontier-038` — explain a FrontierMath-style problem's structure without solving

```text
Act as an expert contest coach. Explain a FrontierMath-style problem's structure without solving for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 38.
```

### `math-frontier-039` — build a probability model with assumptions stated

```text
Act as a patient proof tutor. Build a probability model with assumptions stated for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 39.
```

### `math-frontier-040` — build intuition for eigenvalues with worked cases

```text
Act as a senior statistics mentor. Build intuition for eigenvalues with worked cases for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 40.
```

### `math-frontier-041` — translate a word problem into equations cleanly

```text
Act as an expert discrete-math guide. Translate a word problem into equations cleanly for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 41.
```

### `math-frontier-042` — solve a number-theory problem with modular reasoning

```text
Act as a senior olympiad trainer. Solve a number-theory problem with modular reasoning for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 42.
```

### `math-frontier-043` — verify a proof step by step and flag gaps

```text
Act as a senior mathematician aide. Verify a proof step by step and flag gaps for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 43.
```

### `math-frontier-044` — find a counterexample to a plausible conjecture

```text
Act as an expert contest coach. Find a counterexample to a plausible conjecture for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 44.
```

### `math-frontier-045` — explore prime gaps computationally with a plan

```text
Act as a patient proof tutor. Explore prime gaps computationally with a plan for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 45.
```

### `math-frontier-046` — prove a combinatorial identity with a clear lemma chain

```text
Act as a senior statistics mentor. Prove a combinatorial identity with a clear lemma chain for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 46.
```

### `math-frontier-047` — prove an inequality with named theorems

```text
Act as an expert discrete-math guide. Prove an inequality with named theorems for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 47.
```

### `math-frontier-048` — write a solution sketch plus a full-solution expansion

```text
Act as a senior olympiad trainer. Write a solution sketch plus a full-solution expansion for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 48.
```

### `math-frontier-049` — analyze a recurrence with generating functions

```text
Act as a senior mathematician aide. Analyze a recurrence with generating functions for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 49.
```

### `math-frontier-050` — design a practice set that ramps difficulty

```text
Act as an expert contest coach. Design a practice set that ramps difficulty for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 50.
```

### `math-frontier-051` — design a counting argument with bijection

```text
Act as a patient proof tutor. Design a counting argument with bijection for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 51.
```

### `math-frontier-052` — explain Bayes with a worked medical-test example

```text
Act as a senior statistics mentor. Explain Bayes with a worked medical-test example for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 52.
```

### `math-frontier-053` — explain a FrontierMath-style problem's structure without solving

```text
Act as an expert discrete-math guide. Explain a FrontierMath-style problem's structure without solving for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 53.
```

### `math-frontier-054` — build a probability model with assumptions stated

```text
Act as a senior olympiad trainer. Build a probability model with assumptions stated for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 54.
```

### `math-frontier-055` — build intuition for eigenvalues with worked cases

```text
Act as a senior mathematician aide. Build intuition for eigenvalues with worked cases for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 55.
```

### `math-frontier-056` — translate a word problem into equations cleanly

```text
Act as an expert contest coach. Translate a word problem into equations cleanly for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 56.
```

### `math-frontier-057` — solve a number-theory problem with modular reasoning

```text
Act as a patient proof tutor. Solve a number-theory problem with modular reasoning for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 57.
```

### `math-frontier-058` — verify a proof step by step and flag gaps

```text
Act as a senior statistics mentor. Verify a proof step by step and flag gaps for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 58.
```

### `math-frontier-059` — find a counterexample to a plausible conjecture

```text
Act as an expert discrete-math guide. Find a counterexample to a plausible conjecture for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 59.
```

### `math-frontier-060` — explore prime gaps computationally with a plan

```text
Act as a senior olympiad trainer. Explore prime gaps computationally with a plan for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 60.
```

### `math-frontier-061` — prove a combinatorial identity with a clear lemma chain

```text
Act as a senior mathematician aide. Prove a combinatorial identity with a clear lemma chain for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 61.
```

### `math-frontier-062` — prove an inequality with named theorems

```text
Act as an expert contest coach. Prove an inequality with named theorems for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 62.
```

### `math-frontier-063` — write a solution sketch plus a full-solution expansion

```text
Act as a patient proof tutor. Write a solution sketch plus a full-solution expansion for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 63.
```

### `math-frontier-064` — analyze a recurrence with generating functions

```text
Act as a senior statistics mentor. Analyze a recurrence with generating functions for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 64.
```

### `math-frontier-065` — design a practice set that ramps difficulty

```text
Act as an expert discrete-math guide. Design a practice set that ramps difficulty for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 65.
```

### `math-frontier-066` — design a counting argument with bijection

```text
Act as a senior olympiad trainer. Design a counting argument with bijection for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 66.
```

### `math-frontier-067` — explain Bayes with a worked medical-test example

```text
Act as a senior mathematician aide. Explain Bayes with a worked medical-test example for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 67.
```

### `math-frontier-068` — explain a FrontierMath-style problem's structure without solving

```text
Act as an expert contest coach. Explain a FrontierMath-style problem's structure without solving for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 68.
```

### `math-frontier-069` — build a probability model with assumptions stated

```text
Act as a patient proof tutor. Build a probability model with assumptions stated for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 69.
```

### `math-frontier-070` — build intuition for eigenvalues with worked cases

```text
Act as a senior statistics mentor. Build intuition for eigenvalues with worked cases for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 70.
```

### `math-frontier-071` — translate a word problem into equations cleanly

```text
Act as an expert discrete-math guide. Translate a word problem into equations cleanly for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 71.
```

### `math-frontier-072` — solve a number-theory problem with modular reasoning

```text
Act as a senior olympiad trainer. Solve a number-theory problem with modular reasoning for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 72.
```

### `math-frontier-073` — verify a proof step by step and flag gaps

```text
Act as a senior mathematician aide. Verify a proof step by step and flag gaps for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 73.
```

### `math-frontier-074` — find a counterexample to a plausible conjecture

```text
Act as an expert contest coach. Find a counterexample to a plausible conjecture for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 74.
```

### `math-frontier-075` — explore prime gaps computationally with a plan

```text
Act as a patient proof tutor. Explore prime gaps computationally with a plan for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 75.
```

### `math-frontier-076` — prove a combinatorial identity with a clear lemma chain

```text
Act as a senior statistics mentor. Prove a combinatorial identity with a clear lemma chain for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 76.
```

### `math-frontier-077` — prove an inequality with named theorems

```text
Act as an expert discrete-math guide. Prove an inequality with named theorems for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 77.
```

### `math-frontier-078` — write a solution sketch plus a full-solution expansion

```text
Act as a senior olympiad trainer. Write a solution sketch plus a full-solution expansion for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 78.
```

### `math-frontier-079` — analyze a recurrence with generating functions

```text
Act as a senior mathematician aide. Analyze a recurrence with generating functions for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 79.
```

### `math-frontier-080` — design a practice set that ramps difficulty

```text
Act as an expert contest coach. Design a practice set that ramps difficulty for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 80.
```

### `math-frontier-081` — design a counting argument with bijection

```text
Act as a patient proof tutor. Design a counting argument with bijection for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 81.
```

### `math-frontier-082` — explain Bayes with a worked medical-test example

```text
Act as a senior statistics mentor. Explain Bayes with a worked medical-test example for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 82.
```

### `math-frontier-083` — explain a FrontierMath-style problem's structure without solving

```text
Act as an expert discrete-math guide. Explain a FrontierMath-style problem's structure without solving for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 83.
```

### `math-frontier-084` — build a probability model with assumptions stated

```text
Act as a senior olympiad trainer. Build a probability model with assumptions stated for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 84.
```

### `math-frontier-085` — build intuition for eigenvalues with worked cases

```text
Act as a senior mathematician aide. Build intuition for eigenvalues with worked cases for a contest student. The intended result is a rigorous solution. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 85.
```

### `math-frontier-086` — translate a word problem into equations cleanly

```text
Act as an expert contest coach. Translate a word problem into equations cleanly for a teacher. The intended result is an intuition-plus-proof pair. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 86.
```

### `math-frontier-087` — solve a number-theory problem with modular reasoning

```text
Act as a patient proof tutor. Solve a number-theory problem with modular reasoning for a study group. The intended result is a verification report. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 87.
```

### `math-frontier-088` — verify a proof step by step and flag gaps

```text
Act as a senior statistics mentor. Verify a proof step by step and flag gaps for a curious adult. The intended result is a practice set with hints. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 88.
```

### `math-frontier-089` — find a counterexample to a plausible conjecture

```text
Act as an expert discrete-math guide. Find a counterexample to a plausible conjecture for a researcher. The intended result is a counterexample with check. Requirements: show each logical step; name theorems used; separate proof from intuition explicitly. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 89.
```

### `math-frontier-090` — explore prime gaps computationally with a plan

```text
Act as a senior olympiad trainer. Explore prime gaps computationally with a plan for an undergrad. The intended result is a lemma chain outline. Requirements: check edge cases and small n by hand; no hand-waving; quantify where possible. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 90.
```
