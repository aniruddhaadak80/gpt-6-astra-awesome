# 🖥️ Computer Use & Desktop Automation — 90 copyable GPT-6 Astra prompts

> Astra operating files, apps, forms, calendars, QA flows — with explicit stop points and approvals.

> Safety: defensive / authorized-scope use only. Stop before destructive, irreversible, sending, purchasing, deleting, or permission-changing steps without approval.

**Jump:** [⬆️ Index](../README.md#-category-carousel) · [◀ Prev](01-agentic-coding.md) · [Next ▶](03-browser-research.md) · [🔍 Search all](prompts_index.json) · [🖱️ Interactive carousel](../docs/carousel.html)

Copy any prompt: click the copy icon on its code block.

### `computer-use-001` — fill a multi-page web form from a spreadsheet and confirm submission

```text
Act as a senior desktop-automation specialist. Fill a multi-page web form from a spreadsheet and confirm submission on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `computer-use-002` — audit file permissions in a project folder and produce a fix plan

```text
Act as an expert QA operator. Audit file permissions in a project folder and produce a fix plan under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `computer-use-003` — clean up duplicate photos while preserving originals in an archive

```text
Act as a senior IT support engineer. Clean up duplicate photos while preserving originals in an archive for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `computer-use-004` — prepare a slide deck from a folder of screenshots and notes

```text
Act as an operations automation lead. Prepare a slide deck from a folder of screenshots and notes in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `computer-use-005` — document a reproduceable bug with steps, video timestamps, and logs

```text
Act as a senior systems analyst. Document a reproduceable bug with steps, video timestamps, and logs inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `computer-use-006` — update CRM records from an inbox export without duplicating contacts

```text
Act as an expert accessibility tester. Update CRM records from an inbox export without duplicating contacts on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `computer-use-007` — verify backup integrity by restoring one sample file and checksumming

```text
Act as a senior desktop-automation specialist. Verify backup integrity by restoring one sample file and checksumming on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `computer-use-008` — run frontend QA on a staging site across three viewports and log defects

```text
Act as an expert QA operator. Run frontend QA on a staging site across three viewports and log defects under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `computer-use-009` — migrate bookmarks and settings to a new browser profile safely

```text
Act as a senior IT support engineer. Migrate bookmarks and settings to a new browser profile safely for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `computer-use-010` — install a dev tool, verify the install, and capture screenshots of each 

```text
Act as an operations automation lead. Install a dev tool, verify the install, and capture screenshots of each step in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `computer-use-011` — test a checkout flow with valid, empty, and invalid inputs

```text
Act as a senior systems analyst. Test a checkout flow with valid, empty, and invalid inputs inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `computer-use-012` — organize a downloads folder into a dated archive with a manifest

```text
Act as an expert accessibility tester. Organize a downloads folder into a dated archive with a manifest on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `computer-use-013` — build a weekly report PDF from three source documents

```text
Act as a senior desktop-automation specialist. Build a weekly report PDF from three source documents on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `computer-use-014` — reconcile calendar events across two accounts and resolve conflicts

```text
Act as an expert QA operator. Reconcile calendar events across two accounts and resolve conflicts under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `computer-use-015` — transcribe meeting notes from audio into structured action items

```text
Act as a senior IT support engineer. Transcribe meeting notes from audio into structured action items for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `computer-use-016` — fill a multi-page web form from a spreadsheet and confirm submission

```text
Act as an operations automation lead. Fill a multi-page web form from a spreadsheet and confirm submission in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `computer-use-017` — audit file permissions in a project folder and produce a fix plan

```text
Act as a senior systems analyst. Audit file permissions in a project folder and produce a fix plan inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `computer-use-018` — clean up duplicate photos while preserving originals in an archive

```text
Act as an expert accessibility tester. Clean up duplicate photos while preserving originals in an archive on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `computer-use-019` — prepare a slide deck from a folder of screenshots and notes

```text
Act as a senior desktop-automation specialist. Prepare a slide deck from a folder of screenshots and notes on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `computer-use-020` — document a reproduceable bug with steps, video timestamps, and logs

```text
Act as an expert QA operator. Document a reproduceable bug with steps, video timestamps, and logs under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `computer-use-021` — update CRM records from an inbox export without duplicating contacts

```text
Act as a senior IT support engineer. Update CRM records from an inbox export without duplicating contacts for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `computer-use-022` — verify backup integrity by restoring one sample file and checksumming

```text
Act as an operations automation lead. Verify backup integrity by restoring one sample file and checksumming in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `computer-use-023` — run frontend QA on a staging site across three viewports and log defects

```text
Act as a senior systems analyst. Run frontend QA on a staging site across three viewports and log defects inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `computer-use-024` — migrate bookmarks and settings to a new browser profile safely

```text
Act as an expert accessibility tester. Migrate bookmarks and settings to a new browser profile safely on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `computer-use-025` — install a dev tool, verify the install, and capture screenshots of each 

```text
Act as a senior desktop-automation specialist. Install a dev tool, verify the install, and capture screenshots of each step on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `computer-use-026` — test a checkout flow with valid, empty, and invalid inputs

```text
Act as an expert QA operator. Test a checkout flow with valid, empty, and invalid inputs under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `computer-use-027` — organize a downloads folder into a dated archive with a manifest

```text
Act as a senior IT support engineer. Organize a downloads folder into a dated archive with a manifest for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `computer-use-028` — build a weekly report PDF from three source documents

```text
Act as an operations automation lead. Build a weekly report PDF from three source documents in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `computer-use-029` — reconcile calendar events across two accounts and resolve conflicts

```text
Act as a senior systems analyst. Reconcile calendar events across two accounts and resolve conflicts inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `computer-use-030` — transcribe meeting notes from audio into structured action items

```text
Act as an expert accessibility tester. Transcribe meeting notes from audio into structured action items on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `computer-use-031` — fill a multi-page web form from a spreadsheet and confirm submission

```text
Act as a senior desktop-automation specialist. Fill a multi-page web form from a spreadsheet and confirm submission on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 31.
```

### `computer-use-032` — audit file permissions in a project folder and produce a fix plan

```text
Act as an expert QA operator. Audit file permissions in a project folder and produce a fix plan under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 32.
```

### `computer-use-033` — clean up duplicate photos while preserving originals in an archive

```text
Act as a senior IT support engineer. Clean up duplicate photos while preserving originals in an archive for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 33.
```

### `computer-use-034` — prepare a slide deck from a folder of screenshots and notes

```text
Act as an operations automation lead. Prepare a slide deck from a folder of screenshots and notes in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 34.
```

### `computer-use-035` — document a reproduceable bug with steps, video timestamps, and logs

```text
Act as a senior systems analyst. Document a reproduceable bug with steps, video timestamps, and logs inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 35.
```

### `computer-use-036` — update CRM records from an inbox export without duplicating contacts

```text
Act as an expert accessibility tester. Update CRM records from an inbox export without duplicating contacts on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 36.
```

### `computer-use-037` — verify backup integrity by restoring one sample file and checksumming

```text
Act as a senior desktop-automation specialist. Verify backup integrity by restoring one sample file and checksumming on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 37.
```

### `computer-use-038` — run frontend QA on a staging site across three viewports and log defects

```text
Act as an expert QA operator. Run frontend QA on a staging site across three viewports and log defects under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 38.
```

### `computer-use-039` — migrate bookmarks and settings to a new browser profile safely

```text
Act as a senior IT support engineer. Migrate bookmarks and settings to a new browser profile safely for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 39.
```

### `computer-use-040` — install a dev tool, verify the install, and capture screenshots of each 

```text
Act as an operations automation lead. Install a dev tool, verify the install, and capture screenshots of each step in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 40.
```

### `computer-use-041` — test a checkout flow with valid, empty, and invalid inputs

```text
Act as a senior systems analyst. Test a checkout flow with valid, empty, and invalid inputs inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 41.
```

### `computer-use-042` — organize a downloads folder into a dated archive with a manifest

```text
Act as an expert accessibility tester. Organize a downloads folder into a dated archive with a manifest on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 42.
```

### `computer-use-043` — build a weekly report PDF from three source documents

```text
Act as a senior desktop-automation specialist. Build a weekly report PDF from three source documents on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 43.
```

### `computer-use-044` — reconcile calendar events across two accounts and resolve conflicts

```text
Act as an expert QA operator. Reconcile calendar events across two accounts and resolve conflicts under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 44.
```

### `computer-use-045` — transcribe meeting notes from audio into structured action items

```text
Act as a senior IT support engineer. Transcribe meeting notes from audio into structured action items for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 45.
```

### `computer-use-046` — fill a multi-page web form from a spreadsheet and confirm submission

```text
Act as an operations automation lead. Fill a multi-page web form from a spreadsheet and confirm submission in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 46.
```

### `computer-use-047` — audit file permissions in a project folder and produce a fix plan

```text
Act as a senior systems analyst. Audit file permissions in a project folder and produce a fix plan inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 47.
```

### `computer-use-048` — clean up duplicate photos while preserving originals in an archive

```text
Act as an expert accessibility tester. Clean up duplicate photos while preserving originals in an archive on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 48.
```

### `computer-use-049` — prepare a slide deck from a folder of screenshots and notes

```text
Act as a senior desktop-automation specialist. Prepare a slide deck from a folder of screenshots and notes on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 49.
```

### `computer-use-050` — document a reproduceable bug with steps, video timestamps, and logs

```text
Act as an expert QA operator. Document a reproduceable bug with steps, video timestamps, and logs under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 50.
```

### `computer-use-051` — update CRM records from an inbox export without duplicating contacts

```text
Act as a senior IT support engineer. Update CRM records from an inbox export without duplicating contacts for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 51.
```

### `computer-use-052` — verify backup integrity by restoring one sample file and checksumming

```text
Act as an operations automation lead. Verify backup integrity by restoring one sample file and checksumming in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 52.
```

### `computer-use-053` — run frontend QA on a staging site across three viewports and log defects

```text
Act as a senior systems analyst. Run frontend QA on a staging site across three viewports and log defects inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 53.
```

### `computer-use-054` — migrate bookmarks and settings to a new browser profile safely

```text
Act as an expert accessibility tester. Migrate bookmarks and settings to a new browser profile safely on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 54.
```

### `computer-use-055` — install a dev tool, verify the install, and capture screenshots of each 

```text
Act as a senior desktop-automation specialist. Install a dev tool, verify the install, and capture screenshots of each step on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 55.
```

### `computer-use-056` — test a checkout flow with valid, empty, and invalid inputs

```text
Act as an expert QA operator. Test a checkout flow with valid, empty, and invalid inputs under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 56.
```

### `computer-use-057` — organize a downloads folder into a dated archive with a manifest

```text
Act as a senior IT support engineer. Organize a downloads folder into a dated archive with a manifest for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 57.
```

### `computer-use-058` — build a weekly report PDF from three source documents

```text
Act as an operations automation lead. Build a weekly report PDF from three source documents in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 58.
```

### `computer-use-059` — reconcile calendar events across two accounts and resolve conflicts

```text
Act as a senior systems analyst. Reconcile calendar events across two accounts and resolve conflicts inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 59.
```

### `computer-use-060` — transcribe meeting notes from audio into structured action items

```text
Act as an expert accessibility tester. Transcribe meeting notes from audio into structured action items on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 60.
```

### `computer-use-061` — fill a multi-page web form from a spreadsheet and confirm submission

```text
Act as a senior desktop-automation specialist. Fill a multi-page web form from a spreadsheet and confirm submission on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 61.
```

### `computer-use-062` — audit file permissions in a project folder and produce a fix plan

```text
Act as an expert QA operator. Audit file permissions in a project folder and produce a fix plan under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 62.
```

### `computer-use-063` — clean up duplicate photos while preserving originals in an archive

```text
Act as a senior IT support engineer. Clean up duplicate photos while preserving originals in an archive for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 63.
```

### `computer-use-064` — prepare a slide deck from a folder of screenshots and notes

```text
Act as an operations automation lead. Prepare a slide deck from a folder of screenshots and notes in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 64.
```

### `computer-use-065` — document a reproduceable bug with steps, video timestamps, and logs

```text
Act as a senior systems analyst. Document a reproduceable bug with steps, video timestamps, and logs inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 65.
```

### `computer-use-066` — update CRM records from an inbox export without duplicating contacts

```text
Act as an expert accessibility tester. Update CRM records from an inbox export without duplicating contacts on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 66.
```

### `computer-use-067` — verify backup integrity by restoring one sample file and checksumming

```text
Act as a senior desktop-automation specialist. Verify backup integrity by restoring one sample file and checksumming on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 67.
```

### `computer-use-068` — run frontend QA on a staging site across three viewports and log defects

```text
Act as an expert QA operator. Run frontend QA on a staging site across three viewports and log defects under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 68.
```

### `computer-use-069` — migrate bookmarks and settings to a new browser profile safely

```text
Act as a senior IT support engineer. Migrate bookmarks and settings to a new browser profile safely for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 69.
```

### `computer-use-070` — install a dev tool, verify the install, and capture screenshots of each 

```text
Act as an operations automation lead. Install a dev tool, verify the install, and capture screenshots of each step in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 70.
```

### `computer-use-071` — test a checkout flow with valid, empty, and invalid inputs

```text
Act as a senior systems analyst. Test a checkout flow with valid, empty, and invalid inputs inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 71.
```

### `computer-use-072` — organize a downloads folder into a dated archive with a manifest

```text
Act as an expert accessibility tester. Organize a downloads folder into a dated archive with a manifest on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 72.
```

### `computer-use-073` — build a weekly report PDF from three source documents

```text
Act as a senior desktop-automation specialist. Build a weekly report PDF from three source documents on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 73.
```

### `computer-use-074` — reconcile calendar events across two accounts and resolve conflicts

```text
Act as an expert QA operator. Reconcile calendar events across two accounts and resolve conflicts under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 74.
```

### `computer-use-075` — transcribe meeting notes from audio into structured action items

```text
Act as a senior IT support engineer. Transcribe meeting notes from audio into structured action items for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 75.
```

### `computer-use-076` — fill a multi-page web form from a spreadsheet and confirm submission

```text
Act as an operations automation lead. Fill a multi-page web form from a spreadsheet and confirm submission in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 76.
```

### `computer-use-077` — audit file permissions in a project folder and produce a fix plan

```text
Act as a senior systems analyst. Audit file permissions in a project folder and produce a fix plan inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 77.
```

### `computer-use-078` — clean up duplicate photos while preserving originals in an archive

```text
Act as an expert accessibility tester. Clean up duplicate photos while preserving originals in an archive on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 78.
```

### `computer-use-079` — prepare a slide deck from a folder of screenshots and notes

```text
Act as a senior desktop-automation specialist. Prepare a slide deck from a folder of screenshots and notes on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 79.
```

### `computer-use-080` — document a reproduceable bug with steps, video timestamps, and logs

```text
Act as an expert QA operator. Document a reproduceable bug with steps, video timestamps, and logs under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 80.
```

### `computer-use-081` — update CRM records from an inbox export without duplicating contacts

```text
Act as a senior IT support engineer. Update CRM records from an inbox export without duplicating contacts for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 81.
```

### `computer-use-082` — verify backup integrity by restoring one sample file and checksumming

```text
Act as an operations automation lead. Verify backup integrity by restoring one sample file and checksumming in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 82.
```

### `computer-use-083` — run frontend QA on a staging site across three viewports and log defects

```text
Act as a senior systems analyst. Run frontend QA on a staging site across three viewports and log defects inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 83.
```

### `computer-use-084` — migrate bookmarks and settings to a new browser profile safely

```text
Act as an expert accessibility tester. Migrate bookmarks and settings to a new browser profile safely on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 84.
```

### `computer-use-085` — install a dev tool, verify the install, and capture screenshots of each 

```text
Act as a senior desktop-automation specialist. Install a dev tool, verify the install, and capture screenshots of each step on Windows 11. The intended result is a step log with evidence links and a Done table. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 85.
```

### `computer-use-086` — test a checkout flow with valid, empty, and invalid inputs

```text
Act as an expert QA operator. Test a checkout flow with valid, empty, and invalid inputs under strict least-privilege access. The intended result is an archive folder plus a rollback note. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 86.
```

### `computer-use-087` — organize a downloads folder into a dated archive with a manifest

```text
Act as a senior IT support engineer. Organize a downloads folder into a dated archive with a manifest for a non-technical stakeholder. The intended result is a summary email draft (do not send) with attachments listed. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 87.
```

### `computer-use-088` — build a weekly report PDF from three source documents

```text
Act as an operations automation lead. Build a weekly report PDF from three source documents in a shared team workspace. The intended result is a QA report: tested / failed / unverified with details. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 88.
```

### `computer-use-089` — reconcile calendar events across two accounts and resolve conflicts

```text
Act as a senior systems analyst. Reconcile calendar events across two accounts and resolve conflicts inside a corporate VM. The intended result is a cleaned workspace plus a manifest CSV. Requirements: never delete originals; move removals to an Archive folder; stop before sending, purchasing, booking, deleting, or changing permissions. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 89.
```

### `computer-use-090` — transcribe meeting notes from audio into structured action items

```text
Act as an expert accessibility tester. Transcribe meeting notes from audio into structured action items on macOS. The intended result is a defect list with severity, repro steps, and screenshots. Requirements: capture evidence (screenshots/paths) for every consequential step; work only inside the named folder; do not touch files outside it. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 90.
```
