# 📈 Data Analysis & Visualization — 90 copyable GPT-6 Astra prompts

> From question to chart spec to decision, with honest scales.

> Safety: defensive / authorized-scope use only. Stop before destructive, irreversible, sending, purchasing, deleting, or permission-changing steps without approval.

**Jump:** [⬆️ Index](../README.md#-category-carousel) · [◀ Prev](18-math-frontier.md) · [Next ▶](20-devops-terminal.md) · [🔍 Search all](prompts_index.json) · [🖱️ Interactive carousel](../docs/carousel.html)

Copy any prompt: click the copy icon on its code block.

### `data-viz-001` — analyze a funnel drop with segment breakdowns

```text
Act as a senior data scientist. Analyze a funnel drop with segment breakdowns for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `data-viz-002` — write SQL for a tricky sessionization problem

```text
Act as an expert analytics engineer. Write SQL for a tricky sessionization problem for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `data-viz-003` — write a metrics-definitions doc

```text
Act as a senior BI developer. Write a metrics-definitions doc for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `data-viz-004` — design a dashboard spec with five honest charts

```text
Act as a product analyst. Design a dashboard spec with five honest charts for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `data-viz-005` — build a anomaly-triage playbook

```text
Act as a senior statistician. Build a anomaly-triage playbook for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `data-viz-006` — build a pricing elasticity sketch from history

```text
Act as an expert data storyteller. Build a pricing elasticity sketch from history for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `data-viz-007` — create a executive one-chart memo

```text
Act as a senior data scientist. Create a executive one-chart memo for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `data-viz-008` — analyze survey free-text into themes

```text
Act as an expert analytics engineer. Analyze survey free-text into themes for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `data-viz-009` — design an experiment backlog scored by ICE

```text
Act as a senior BI developer. Design an experiment backlog scored by ICE for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `data-viz-010` — create a forecast with scenarios and assumptions

```text
Act as a product analyst. Create a forecast with scenarios and assumptions for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `data-viz-011` — analyze support tickets for top drivers

```text
Act as a senior statistician. Analyze support tickets for top drivers for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `data-viz-012` — design an A/B read-out with guardrails

```text
Act as an expert data storyteller. Design an A/B read-out with guardrails for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `data-viz-013` — build a cohort table from event logs

```text
Act as a senior data scientist. Build a cohort table from event logs for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `data-viz-014` — build a churn analysis with leading indicators

```text
Act as an expert analytics engineer. Build a churn analysis with leading indicators for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `data-viz-015` — plan a data-quality audit with check queries

```text
Act as a senior BI developer. Plan a data-quality audit with check queries for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `data-viz-016` — analyze a funnel drop with segment breakdowns

```text
Act as a product analyst. Analyze a funnel drop with segment breakdowns for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `data-viz-017` — write SQL for a tricky sessionization problem

```text
Act as a senior statistician. Write SQL for a tricky sessionization problem for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `data-viz-018` — write a metrics-definitions doc

```text
Act as an expert data storyteller. Write a metrics-definitions doc for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `data-viz-019` — design a dashboard spec with five honest charts

```text
Act as a senior data scientist. Design a dashboard spec with five honest charts for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `data-viz-020` — build a anomaly-triage playbook

```text
Act as an expert analytics engineer. Build a anomaly-triage playbook for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `data-viz-021` — build a pricing elasticity sketch from history

```text
Act as a senior BI developer. Build a pricing elasticity sketch from history for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `data-viz-022` — create a executive one-chart memo

```text
Act as a product analyst. Create a executive one-chart memo for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `data-viz-023` — analyze survey free-text into themes

```text
Act as a senior statistician. Analyze survey free-text into themes for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `data-viz-024` — design an experiment backlog scored by ICE

```text
Act as an expert data storyteller. Design an experiment backlog scored by ICE for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `data-viz-025` — create a forecast with scenarios and assumptions

```text
Act as a senior data scientist. Create a forecast with scenarios and assumptions for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `data-viz-026` — analyze support tickets for top drivers

```text
Act as an expert analytics engineer. Analyze support tickets for top drivers for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `data-viz-027` — design an A/B read-out with guardrails

```text
Act as a senior BI developer. Design an A/B read-out with guardrails for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `data-viz-028` — build a cohort table from event logs

```text
Act as a product analyst. Build a cohort table from event logs for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `data-viz-029` — build a churn analysis with leading indicators

```text
Act as a senior statistician. Build a churn analysis with leading indicators for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `data-viz-030` — plan a data-quality audit with check queries

```text
Act as an expert data storyteller. Plan a data-quality audit with check queries for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `data-viz-031` — analyze a funnel drop with segment breakdowns

```text
Act as a senior data scientist. Analyze a funnel drop with segment breakdowns for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 31.
```

### `data-viz-032` — write SQL for a tricky sessionization problem

```text
Act as an expert analytics engineer. Write SQL for a tricky sessionization problem for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 32.
```

### `data-viz-033` — write a metrics-definitions doc

```text
Act as a senior BI developer. Write a metrics-definitions doc for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 33.
```

### `data-viz-034` — design a dashboard spec with five honest charts

```text
Act as a product analyst. Design a dashboard spec with five honest charts for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 34.
```

### `data-viz-035` — build a anomaly-triage playbook

```text
Act as a senior statistician. Build a anomaly-triage playbook for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 35.
```

### `data-viz-036` — build a pricing elasticity sketch from history

```text
Act as an expert data storyteller. Build a pricing elasticity sketch from history for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 36.
```

### `data-viz-037` — create a executive one-chart memo

```text
Act as a senior data scientist. Create a executive one-chart memo for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 37.
```

### `data-viz-038` — analyze survey free-text into themes

```text
Act as an expert analytics engineer. Analyze survey free-text into themes for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 38.
```

### `data-viz-039` — design an experiment backlog scored by ICE

```text
Act as a senior BI developer. Design an experiment backlog scored by ICE for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 39.
```

### `data-viz-040` — create a forecast with scenarios and assumptions

```text
Act as a product analyst. Create a forecast with scenarios and assumptions for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 40.
```

### `data-viz-041` — analyze support tickets for top drivers

```text
Act as a senior statistician. Analyze support tickets for top drivers for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 41.
```

### `data-viz-042` — design an A/B read-out with guardrails

```text
Act as an expert data storyteller. Design an A/B read-out with guardrails for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 42.
```

### `data-viz-043` — build a cohort table from event logs

```text
Act as a senior data scientist. Build a cohort table from event logs for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 43.
```

### `data-viz-044` — build a churn analysis with leading indicators

```text
Act as an expert analytics engineer. Build a churn analysis with leading indicators for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 44.
```

### `data-viz-045` — plan a data-quality audit with check queries

```text
Act as a senior BI developer. Plan a data-quality audit with check queries for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 45.
```

### `data-viz-046` — analyze a funnel drop with segment breakdowns

```text
Act as a product analyst. Analyze a funnel drop with segment breakdowns for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 46.
```

### `data-viz-047` — write SQL for a tricky sessionization problem

```text
Act as a senior statistician. Write SQL for a tricky sessionization problem for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 47.
```

### `data-viz-048` — write a metrics-definitions doc

```text
Act as an expert data storyteller. Write a metrics-definitions doc for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 48.
```

### `data-viz-049` — design a dashboard spec with five honest charts

```text
Act as a senior data scientist. Design a dashboard spec with five honest charts for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 49.
```

### `data-viz-050` — build a anomaly-triage playbook

```text
Act as an expert analytics engineer. Build a anomaly-triage playbook for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 50.
```

### `data-viz-051` — build a pricing elasticity sketch from history

```text
Act as a senior BI developer. Build a pricing elasticity sketch from history for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 51.
```

### `data-viz-052` — create a executive one-chart memo

```text
Act as a product analyst. Create a executive one-chart memo for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 52.
```

### `data-viz-053` — analyze survey free-text into themes

```text
Act as a senior statistician. Analyze survey free-text into themes for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 53.
```

### `data-viz-054` — design an experiment backlog scored by ICE

```text
Act as an expert data storyteller. Design an experiment backlog scored by ICE for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 54.
```

### `data-viz-055` — create a forecast with scenarios and assumptions

```text
Act as a senior data scientist. Create a forecast with scenarios and assumptions for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 55.
```

### `data-viz-056` — analyze support tickets for top drivers

```text
Act as an expert analytics engineer. Analyze support tickets for top drivers for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 56.
```

### `data-viz-057` — design an A/B read-out with guardrails

```text
Act as a senior BI developer. Design an A/B read-out with guardrails for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 57.
```

### `data-viz-058` — build a cohort table from event logs

```text
Act as a product analyst. Build a cohort table from event logs for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 58.
```

### `data-viz-059` — build a churn analysis with leading indicators

```text
Act as a senior statistician. Build a churn analysis with leading indicators for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 59.
```

### `data-viz-060` — plan a data-quality audit with check queries

```text
Act as an expert data storyteller. Plan a data-quality audit with check queries for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 60.
```

### `data-viz-061` — analyze a funnel drop with segment breakdowns

```text
Act as a senior data scientist. Analyze a funnel drop with segment breakdowns for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 61.
```

### `data-viz-062` — write SQL for a tricky sessionization problem

```text
Act as an expert analytics engineer. Write SQL for a tricky sessionization problem for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 62.
```

### `data-viz-063` — write a metrics-definitions doc

```text
Act as a senior BI developer. Write a metrics-definitions doc for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 63.
```

### `data-viz-064` — design a dashboard spec with five honest charts

```text
Act as a product analyst. Design a dashboard spec with five honest charts for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 64.
```

### `data-viz-065` — build a anomaly-triage playbook

```text
Act as a senior statistician. Build a anomaly-triage playbook for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 65.
```

### `data-viz-066` — build a pricing elasticity sketch from history

```text
Act as an expert data storyteller. Build a pricing elasticity sketch from history for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 66.
```

### `data-viz-067` — create a executive one-chart memo

```text
Act as a senior data scientist. Create a executive one-chart memo for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 67.
```

### `data-viz-068` — analyze survey free-text into themes

```text
Act as an expert analytics engineer. Analyze survey free-text into themes for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 68.
```

### `data-viz-069` — design an experiment backlog scored by ICE

```text
Act as a senior BI developer. Design an experiment backlog scored by ICE for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 69.
```

### `data-viz-070` — create a forecast with scenarios and assumptions

```text
Act as a product analyst. Create a forecast with scenarios and assumptions for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 70.
```

### `data-viz-071` — analyze support tickets for top drivers

```text
Act as a senior statistician. Analyze support tickets for top drivers for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 71.
```

### `data-viz-072` — design an A/B read-out with guardrails

```text
Act as an expert data storyteller. Design an A/B read-out with guardrails for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 72.
```

### `data-viz-073` — build a cohort table from event logs

```text
Act as a senior data scientist. Build a cohort table from event logs for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 73.
```

### `data-viz-074` — build a churn analysis with leading indicators

```text
Act as an expert analytics engineer. Build a churn analysis with leading indicators for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 74.
```

### `data-viz-075` — plan a data-quality audit with check queries

```text
Act as a senior BI developer. Plan a data-quality audit with check queries for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 75.
```

### `data-viz-076` — analyze a funnel drop with segment breakdowns

```text
Act as a product analyst. Analyze a funnel drop with segment breakdowns for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 76.
```

### `data-viz-077` — write SQL for a tricky sessionization problem

```text
Act as a senior statistician. Write SQL for a tricky sessionization problem for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 77.
```

### `data-viz-078` — write a metrics-definitions doc

```text
Act as an expert data storyteller. Write a metrics-definitions doc for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 78.
```

### `data-viz-079` — design a dashboard spec with five honest charts

```text
Act as a senior data scientist. Design a dashboard spec with five honest charts for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 79.
```

### `data-viz-080` — build a anomaly-triage playbook

```text
Act as an expert analytics engineer. Build a anomaly-triage playbook for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 80.
```

### `data-viz-081` — build a pricing elasticity sketch from history

```text
Act as a senior BI developer. Build a pricing elasticity sketch from history for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 81.
```

### `data-viz-082` — create a executive one-chart memo

```text
Act as a product analyst. Create a executive one-chart memo for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 82.
```

### `data-viz-083` — analyze survey free-text into themes

```text
Act as a senior statistician. Analyze survey free-text into themes for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 83.
```

### `data-viz-084` — design an experiment backlog scored by ICE

```text
Act as an expert data storyteller. Design an experiment backlog scored by ICE for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 84.
```

### `data-viz-085` — create a forecast with scenarios and assumptions

```text
Act as a senior data scientist. Create a forecast with scenarios and assumptions for a product team. The intended result is an analysis memo with a recommendation. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 85.
```

### `data-viz-086` — analyze support tickets for top drivers

```text
Act as an expert analytics engineer. Analyze support tickets for top drivers for a classroom. The intended result is a next-questions list. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 86.
```

### `data-viz-087` — design an A/B read-out with guardrails

```text
Act as a senior BI developer. Design an A/B read-out with guardrails for a nonprofit. The intended result is a metrics dictionary. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 87.
```

### `data-viz-088` — build a cohort table from event logs

```text
Act as a product analyst. Build a cohort table from event logs for finance. The intended result is a dashboard wireframe description. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 88.
```

### `data-viz-089` — build a churn analysis with leading indicators

```text
Act as a senior statistician. Build a churn analysis with leading indicators for marketing. The intended result is SQL plus validation queries. Requirements: state the question, data, grain, and date range first; honest axes; no truncated-y tricks without labels. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 89.
```

### `data-viz-090` — plan a data-quality audit with check queries

```text
Act as an expert data storyteller. Plan a data-quality audit with check queries for execs. The intended result is a chart spec with columns and filters. Requirements: note missing data and biases explicitly; reproducible steps: queries plus check totals. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 90.
```
