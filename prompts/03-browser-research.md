# 🌐 Browser Research & Deep Dives — 90 copyable GPT-6 Astra prompts

> Multi-site research, comparisons, citations, and monitoring with source hygiene.

> Safety: defensive / authorized-scope use only. Stop before destructive, irreversible, sending, purchasing, deleting, or permission-changing steps without approval.

**Jump:** [⬆️ Index](../README.md#-category-carousel) · [◀ Prev](02-computer-use.md) · [Next ▶](04-professional-docs.md) · [🔍 Search all](prompts_index.json) · [🖱️ Interactive carousel](../docs/carousel.html)

Copy any prompt: click the copy icon on its code block.

### `browser-research-001` — compare five tools on pricing, limits, and API quality with sources

```text
Act as a senior research analyst. Compare five tools on pricing, limits, and API quality with sources for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `browser-research-002` — build a reading list with difficulty tiers and prerequisites

```text
Act as an expert OSINT researcher. Build a reading list with difficulty tiers and prerequisites for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `browser-research-003` — create a timeline of incidents with root causes and fixes

```text
Act as a principal market analyst. Create a timeline of incidents with root causes and fixes for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `browser-research-004` — verify a viral claim against primary evidence and rate confidence

```text
Act as a senior technical writer. Verify a viral claim against primary evidence and rate confidence for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `browser-research-005` — summarize academic papers into decision-ready memos

```text
Act as an expert fact-checker. Summarize academic papers into decision-ready memos for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `browser-research-006` — collect real user complaints about a product from forums with quotes

```text
Act as a senior competitive-intelligence analyst. Collect real user complaints about a product from forums with quotes for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `browser-research-007` — build a glossary from authoritative definitions with sources

```text
Act as a senior research analyst. Build a glossary from authoritative definitions with sources for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `browser-research-008` — map regulatory requirements across three regions with official links

```text
Act as an expert OSINT researcher. Map regulatory requirements across three regions with official links for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `browser-research-009` — find three independent confirmations for each key fact

```text
Act as a principal market analyst. Find three independent confirmations for each key fact for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `browser-research-010` — summarize ten long articles into a cited briefing with disagreements fla

```text
Act as a senior technical writer. Summarize ten long articles into a cited briefing with disagreements flagged for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `browser-research-011` — benchmark framework docs for a migration decision

```text
Act as an expert fact-checker. Benchmark framework docs for a migration decision for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `browser-research-012` — build a vendor shortlist with scored criteria and disqualifiers

```text
Act as a senior competitive-intelligence analyst. Build a vendor shortlist with scored criteria and disqualifiers for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `browser-research-013` — extract dataset licenses and use-limits into a compliance table

```text
Act as a senior research analyst. Extract dataset licenses and use-limits into a compliance table for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `browser-research-014` — trace the launch timeline of a model from primary sources only

```text
Act as an expert OSINT researcher. Trace the launch timeline of a model from primary sources only for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `browser-research-015` — monitor three competitor changelogs and summarize deltas weekly

```text
Act as a principal market analyst. Monitor three competitor changelogs and summarize deltas weekly for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `browser-research-016` — compare five tools on pricing, limits, and API quality with sources

```text
Act as a senior technical writer. Compare five tools on pricing, limits, and API quality with sources for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `browser-research-017` — build a reading list with difficulty tiers and prerequisites

```text
Act as an expert fact-checker. Build a reading list with difficulty tiers and prerequisites for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `browser-research-018` — create a timeline of incidents with root causes and fixes

```text
Act as a senior competitive-intelligence analyst. Create a timeline of incidents with root causes and fixes for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `browser-research-019` — verify a viral claim against primary evidence and rate confidence

```text
Act as a senior research analyst. Verify a viral claim against primary evidence and rate confidence for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `browser-research-020` — summarize academic papers into decision-ready memos

```text
Act as an expert OSINT researcher. Summarize academic papers into decision-ready memos for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `browser-research-021` — collect real user complaints about a product from forums with quotes

```text
Act as a principal market analyst. Collect real user complaints about a product from forums with quotes for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `browser-research-022` — build a glossary from authoritative definitions with sources

```text
Act as a senior technical writer. Build a glossary from authoritative definitions with sources for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `browser-research-023` — map regulatory requirements across three regions with official links

```text
Act as an expert fact-checker. Map regulatory requirements across three regions with official links for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `browser-research-024` — find three independent confirmations for each key fact

```text
Act as a senior competitive-intelligence analyst. Find three independent confirmations for each key fact for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `browser-research-025` — summarize ten long articles into a cited briefing with disagreements fla

```text
Act as a senior research analyst. Summarize ten long articles into a cited briefing with disagreements flagged for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `browser-research-026` — benchmark framework docs for a migration decision

```text
Act as an expert OSINT researcher. Benchmark framework docs for a migration decision for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `browser-research-027` — build a vendor shortlist with scored criteria and disqualifiers

```text
Act as a principal market analyst. Build a vendor shortlist with scored criteria and disqualifiers for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `browser-research-028` — extract dataset licenses and use-limits into a compliance table

```text
Act as a senior technical writer. Extract dataset licenses and use-limits into a compliance table for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `browser-research-029` — trace the launch timeline of a model from primary sources only

```text
Act as an expert fact-checker. Trace the launch timeline of a model from primary sources only for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `browser-research-030` — monitor three competitor changelogs and summarize deltas weekly

```text
Act as a senior competitive-intelligence analyst. Monitor three competitor changelogs and summarize deltas weekly for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `browser-research-031` — compare five tools on pricing, limits, and API quality with sources

```text
Act as a senior research analyst. Compare five tools on pricing, limits, and API quality with sources for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 31.
```

### `browser-research-032` — build a reading list with difficulty tiers and prerequisites

```text
Act as an expert OSINT researcher. Build a reading list with difficulty tiers and prerequisites for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 32.
```

### `browser-research-033` — create a timeline of incidents with root causes and fixes

```text
Act as a principal market analyst. Create a timeline of incidents with root causes and fixes for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 33.
```

### `browser-research-034` — verify a viral claim against primary evidence and rate confidence

```text
Act as a senior technical writer. Verify a viral claim against primary evidence and rate confidence for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 34.
```

### `browser-research-035` — summarize academic papers into decision-ready memos

```text
Act as an expert fact-checker. Summarize academic papers into decision-ready memos for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 35.
```

### `browser-research-036` — collect real user complaints about a product from forums with quotes

```text
Act as a senior competitive-intelligence analyst. Collect real user complaints about a product from forums with quotes for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 36.
```

### `browser-research-037` — build a glossary from authoritative definitions with sources

```text
Act as a senior research analyst. Build a glossary from authoritative definitions with sources for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 37.
```

### `browser-research-038` — map regulatory requirements across three regions with official links

```text
Act as an expert OSINT researcher. Map regulatory requirements across three regions with official links for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 38.
```

### `browser-research-039` — find three independent confirmations for each key fact

```text
Act as a principal market analyst. Find three independent confirmations for each key fact for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 39.
```

### `browser-research-040` — summarize ten long articles into a cited briefing with disagreements fla

```text
Act as a senior technical writer. Summarize ten long articles into a cited briefing with disagreements flagged for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 40.
```

### `browser-research-041` — benchmark framework docs for a migration decision

```text
Act as an expert fact-checker. Benchmark framework docs for a migration decision for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 41.
```

### `browser-research-042` — build a vendor shortlist with scored criteria and disqualifiers

```text
Act as a senior competitive-intelligence analyst. Build a vendor shortlist with scored criteria and disqualifiers for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 42.
```

### `browser-research-043` — extract dataset licenses and use-limits into a compliance table

```text
Act as a senior research analyst. Extract dataset licenses and use-limits into a compliance table for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 43.
```

### `browser-research-044` — trace the launch timeline of a model from primary sources only

```text
Act as an expert OSINT researcher. Trace the launch timeline of a model from primary sources only for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 44.
```

### `browser-research-045` — monitor three competitor changelogs and summarize deltas weekly

```text
Act as a principal market analyst. Monitor three competitor changelogs and summarize deltas weekly for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 45.
```

### `browser-research-046` — compare five tools on pricing, limits, and API quality with sources

```text
Act as a senior technical writer. Compare five tools on pricing, limits, and API quality with sources for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 46.
```

### `browser-research-047` — build a reading list with difficulty tiers and prerequisites

```text
Act as an expert fact-checker. Build a reading list with difficulty tiers and prerequisites for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 47.
```

### `browser-research-048` — create a timeline of incidents with root causes and fixes

```text
Act as a senior competitive-intelligence analyst. Create a timeline of incidents with root causes and fixes for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 48.
```

### `browser-research-049` — verify a viral claim against primary evidence and rate confidence

```text
Act as a senior research analyst. Verify a viral claim against primary evidence and rate confidence for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 49.
```

### `browser-research-050` — summarize academic papers into decision-ready memos

```text
Act as an expert OSINT researcher. Summarize academic papers into decision-ready memos for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 50.
```

### `browser-research-051` — collect real user complaints about a product from forums with quotes

```text
Act as a principal market analyst. Collect real user complaints about a product from forums with quotes for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 51.
```

### `browser-research-052` — build a glossary from authoritative definitions with sources

```text
Act as a senior technical writer. Build a glossary from authoritative definitions with sources for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 52.
```

### `browser-research-053` — map regulatory requirements across three regions with official links

```text
Act as an expert fact-checker. Map regulatory requirements across three regions with official links for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 53.
```

### `browser-research-054` — find three independent confirmations for each key fact

```text
Act as a senior competitive-intelligence analyst. Find three independent confirmations for each key fact for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 54.
```

### `browser-research-055` — summarize ten long articles into a cited briefing with disagreements fla

```text
Act as a senior research analyst. Summarize ten long articles into a cited briefing with disagreements flagged for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 55.
```

### `browser-research-056` — benchmark framework docs for a migration decision

```text
Act as an expert OSINT researcher. Benchmark framework docs for a migration decision for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 56.
```

### `browser-research-057` — build a vendor shortlist with scored criteria and disqualifiers

```text
Act as a principal market analyst. Build a vendor shortlist with scored criteria and disqualifiers for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 57.
```

### `browser-research-058` — extract dataset licenses and use-limits into a compliance table

```text
Act as a senior technical writer. Extract dataset licenses and use-limits into a compliance table for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 58.
```

### `browser-research-059` — trace the launch timeline of a model from primary sources only

```text
Act as an expert fact-checker. Trace the launch timeline of a model from primary sources only for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 59.
```

### `browser-research-060` — monitor three competitor changelogs and summarize deltas weekly

```text
Act as a senior competitive-intelligence analyst. Monitor three competitor changelogs and summarize deltas weekly for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 60.
```

### `browser-research-061` — compare five tools on pricing, limits, and API quality with sources

```text
Act as a senior research analyst. Compare five tools on pricing, limits, and API quality with sources for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 61.
```

### `browser-research-062` — build a reading list with difficulty tiers and prerequisites

```text
Act as an expert OSINT researcher. Build a reading list with difficulty tiers and prerequisites for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 62.
```

### `browser-research-063` — create a timeline of incidents with root causes and fixes

```text
Act as a principal market analyst. Create a timeline of incidents with root causes and fixes for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 63.
```

### `browser-research-064` — verify a viral claim against primary evidence and rate confidence

```text
Act as a senior technical writer. Verify a viral claim against primary evidence and rate confidence for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 64.
```

### `browser-research-065` — summarize academic papers into decision-ready memos

```text
Act as an expert fact-checker. Summarize academic papers into decision-ready memos for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 65.
```

### `browser-research-066` — collect real user complaints about a product from forums with quotes

```text
Act as a senior competitive-intelligence analyst. Collect real user complaints about a product from forums with quotes for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 66.
```

### `browser-research-067` — build a glossary from authoritative definitions with sources

```text
Act as a senior research analyst. Build a glossary from authoritative definitions with sources for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 67.
```

### `browser-research-068` — map regulatory requirements across three regions with official links

```text
Act as an expert OSINT researcher. Map regulatory requirements across three regions with official links for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 68.
```

### `browser-research-069` — find three independent confirmations for each key fact

```text
Act as a principal market analyst. Find three independent confirmations for each key fact for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 69.
```

### `browser-research-070` — summarize ten long articles into a cited briefing with disagreements fla

```text
Act as a senior technical writer. Summarize ten long articles into a cited briefing with disagreements flagged for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 70.
```

### `browser-research-071` — benchmark framework docs for a migration decision

```text
Act as an expert fact-checker. Benchmark framework docs for a migration decision for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 71.
```

### `browser-research-072` — build a vendor shortlist with scored criteria and disqualifiers

```text
Act as a senior competitive-intelligence analyst. Build a vendor shortlist with scored criteria and disqualifiers for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 72.
```

### `browser-research-073` — extract dataset licenses and use-limits into a compliance table

```text
Act as a senior research analyst. Extract dataset licenses and use-limits into a compliance table for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 73.
```

### `browser-research-074` — trace the launch timeline of a model from primary sources only

```text
Act as an expert OSINT researcher. Trace the launch timeline of a model from primary sources only for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 74.
```

### `browser-research-075` — monitor three competitor changelogs and summarize deltas weekly

```text
Act as a principal market analyst. Monitor three competitor changelogs and summarize deltas weekly for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 75.
```

### `browser-research-076` — compare five tools on pricing, limits, and API quality with sources

```text
Act as a senior technical writer. Compare five tools on pricing, limits, and API quality with sources for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 76.
```

### `browser-research-077` — build a reading list with difficulty tiers and prerequisites

```text
Act as an expert fact-checker. Build a reading list with difficulty tiers and prerequisites for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 77.
```

### `browser-research-078` — create a timeline of incidents with root causes and fixes

```text
Act as a senior competitive-intelligence analyst. Create a timeline of incidents with root causes and fixes for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 78.
```

### `browser-research-079` — verify a viral claim against primary evidence and rate confidence

```text
Act as a senior research analyst. Verify a viral claim against primary evidence and rate confidence for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 79.
```

### `browser-research-080` — summarize academic papers into decision-ready memos

```text
Act as an expert OSINT researcher. Summarize academic papers into decision-ready memos for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 80.
```

### `browser-research-081` — collect real user complaints about a product from forums with quotes

```text
Act as a principal market analyst. Collect real user complaints about a product from forums with quotes for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 81.
```

### `browser-research-082` — build a glossary from authoritative definitions with sources

```text
Act as a senior technical writer. Build a glossary from authoritative definitions with sources for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 82.
```

### `browser-research-083` — map regulatory requirements across three regions with official links

```text
Act as an expert fact-checker. Map regulatory requirements across three regions with official links for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 83.
```

### `browser-research-084` — find three independent confirmations for each key fact

```text
Act as a senior competitive-intelligence analyst. Find three independent confirmations for each key fact for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 84.
```

### `browser-research-085` — summarize ten long articles into a cited briefing with disagreements fla

```text
Act as a senior research analyst. Summarize ten long articles into a cited briefing with disagreements flagged for a CTO decision. The intended result is a cited briefing with a confidence column. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 85.
```

### `browser-research-086` — benchmark framework docs for a migration decision

```text
Act as an expert OSINT researcher. Benchmark framework docs for a migration decision for an investor memo. The intended result is a CSV of sources with title, URL, date, and quote. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 86.
```

### `browser-research-087` — build a vendor shortlist with scored criteria and disqualifiers

```text
Act as a principal market analyst. Build a vendor shortlist with scored criteria and disqualifiers for a classroom. The intended result is a decision memo ending in a clear recommendation. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 87.
```

### `browser-research-088` — extract dataset licenses and use-limits into a compliance table

```text
Act as a senior technical writer. Extract dataset licenses and use-limits into a compliance table for a startup pitch. The intended result is an annotated link list with one-line verdicts. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 88.
```

### `browser-research-089` — trace the launch timeline of a model from primary sources only

```text
Act as an expert fact-checker. Trace the launch timeline of a model from primary sources only for a legal review. The intended result is a timeline with sources per event. Requirements: cite every non-obvious claim with a link; mark confidence high/medium/low; prefer primary sources; label secondary inference explicitly. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 89.
```

### `browser-research-090` — monitor three competitor changelogs and summarize deltas weekly

```text
Act as a senior competitive-intelligence analyst. Monitor three competitor changelogs and summarize deltas weekly for a product team. The intended result is a comparison table with winner per criterion. Requirements: exclude paywalled speculation unless corroborated; record access dates for volatile pages. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 90.
```
