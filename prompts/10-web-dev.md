# 🧑‍💻 Web Dev & Sites in ChatGPT — 90 copyable GPT-6 Astra prompts

> Premium sites, booking flows, SEO copy, frontend QA Astra can self-check.

> Safety: defensive / authorized-scope use only. Stop before destructive, irreversible, sending, purchasing, deleting, or permission-changing steps without approval.

**Jump:** [⬆️ Index](../README.md#-category-carousel) · [◀ Prev](09-game-dev.md) · [Next ▶](11-video-creation.md) · [🔍 Search all](prompts_index.json) · [🖱️ Interactive carousel](../docs/carousel.html)

Copy any prompt: click the copy icon on its code block.

### `web-dev-001` — build a luxury auto-detailing site with booking flow

```text
Act as a senior frontend engineer. Build a luxury auto-detailing site with booking flow for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `web-dev-002` — create a course landing page with curriculum and outcomes

```text
Act as an expert web designer. Create a course landing page with curriculum and outcomes for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `web-dev-003` — run a frontend QA pass: nav, forms, mobile, a11y

```text
Act as a senior conversion strategist. Run a frontend QA pass: nav, forms, mobile, a11y for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `web-dev-004` — build a real-estate listing page with gallery and tour CTA

```text
Act as a full-stack web developer. Build a real-estate listing page with gallery and tour CTA for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `web-dev-005` — create a pricing-page A/B test plan with hypotheses

```text
Act as a senior UX engineer. Create a pricing-page A/B test plan with hypotheses for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `web-dev-006` — create an event site with schedule, speakers, and tickets

```text
Act as an expert SEO copywriter. Create an event site with schedule, speakers, and tickets for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `web-dev-007` — build a waitlist page with referral mechanics

```text
Act as a senior frontend engineer. Build a waitlist page with referral mechanics for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `web-dev-008` — build a clinic booking site with forms and reminders copy

```text
Act as an expert web designer. Build a clinic booking site with forms and reminders copy for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `web-dev-009` — create a changelog page with version filters

```text
Act as a senior conversion strategist. Create a changelog page with version filters for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `web-dev-010` — create a portfolio site with case-study templates

```text
Act as a full-stack web developer. Create a portfolio site with case-study templates for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `web-dev-011` — build a docs site with search-friendly structure

```text
Act as a senior UX engineer. Build a docs site with search-friendly structure for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `web-dev-012` — build a SaaS landing page with pricing and FAQ

```text
Act as an expert SEO copywriter. Build a SaaS landing page with pricing and FAQ for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `web-dev-013` — create a job-board page with filters and alerts copy

```text
Act as a senior frontend engineer. Create a job-board page with filters and alerts copy for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `web-dev-014` — create a restaurant site with menu, reservations, and local SEO

```text
Act as an expert web designer. Create a restaurant site with menu, reservations, and local SEO for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `web-dev-015` — build a nonprofit donation page with impact stories

```text
Act as a senior conversion strategist. Build a nonprofit donation page with impact stories for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `web-dev-016` — build a luxury auto-detailing site with booking flow

```text
Act as a full-stack web developer. Build a luxury auto-detailing site with booking flow for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `web-dev-017` — create a course landing page with curriculum and outcomes

```text
Act as a senior UX engineer. Create a course landing page with curriculum and outcomes for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `web-dev-018` — run a frontend QA pass: nav, forms, mobile, a11y

```text
Act as an expert SEO copywriter. Run a frontend QA pass: nav, forms, mobile, a11y for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `web-dev-019` — build a real-estate listing page with gallery and tour CTA

```text
Act as a senior frontend engineer. Build a real-estate listing page with gallery and tour CTA for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `web-dev-020` — create a pricing-page A/B test plan with hypotheses

```text
Act as an expert web designer. Create a pricing-page A/B test plan with hypotheses for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `web-dev-021` — create an event site with schedule, speakers, and tickets

```text
Act as a senior conversion strategist. Create an event site with schedule, speakers, and tickets for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `web-dev-022` — build a waitlist page with referral mechanics

```text
Act as a full-stack web developer. Build a waitlist page with referral mechanics for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `web-dev-023` — build a clinic booking site with forms and reminders copy

```text
Act as a senior UX engineer. Build a clinic booking site with forms and reminders copy for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `web-dev-024` — create a changelog page with version filters

```text
Act as an expert SEO copywriter. Create a changelog page with version filters for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `web-dev-025` — create a portfolio site with case-study templates

```text
Act as a senior frontend engineer. Create a portfolio site with case-study templates for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `web-dev-026` — build a docs site with search-friendly structure

```text
Act as an expert web designer. Build a docs site with search-friendly structure for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `web-dev-027` — build a SaaS landing page with pricing and FAQ

```text
Act as a senior conversion strategist. Build a SaaS landing page with pricing and FAQ for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `web-dev-028` — create a job-board page with filters and alerts copy

```text
Act as a full-stack web developer. Create a job-board page with filters and alerts copy for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `web-dev-029` — create a restaurant site with menu, reservations, and local SEO

```text
Act as a senior UX engineer. Create a restaurant site with menu, reservations, and local SEO for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `web-dev-030` — build a nonprofit donation page with impact stories

```text
Act as an expert SEO copywriter. Build a nonprofit donation page with impact stories for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `web-dev-031` — build a luxury auto-detailing site with booking flow

```text
Act as a senior frontend engineer. Build a luxury auto-detailing site with booking flow for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 31.
```

### `web-dev-032` — create a course landing page with curriculum and outcomes

```text
Act as an expert web designer. Create a course landing page with curriculum and outcomes for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 32.
```

### `web-dev-033` — run a frontend QA pass: nav, forms, mobile, a11y

```text
Act as a senior conversion strategist. Run a frontend QA pass: nav, forms, mobile, a11y for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 33.
```

### `web-dev-034` — build a real-estate listing page with gallery and tour CTA

```text
Act as a full-stack web developer. Build a real-estate listing page with gallery and tour CTA for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 34.
```

### `web-dev-035` — create a pricing-page A/B test plan with hypotheses

```text
Act as a senior UX engineer. Create a pricing-page A/B test plan with hypotheses for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 35.
```

### `web-dev-036` — create an event site with schedule, speakers, and tickets

```text
Act as an expert SEO copywriter. Create an event site with schedule, speakers, and tickets for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 36.
```

### `web-dev-037` — build a waitlist page with referral mechanics

```text
Act as a senior frontend engineer. Build a waitlist page with referral mechanics for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 37.
```

### `web-dev-038` — build a clinic booking site with forms and reminders copy

```text
Act as an expert web designer. Build a clinic booking site with forms and reminders copy for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 38.
```

### `web-dev-039` — create a changelog page with version filters

```text
Act as a senior conversion strategist. Create a changelog page with version filters for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 39.
```

### `web-dev-040` — create a portfolio site with case-study templates

```text
Act as a full-stack web developer. Create a portfolio site with case-study templates for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 40.
```

### `web-dev-041` — build a docs site with search-friendly structure

```text
Act as a senior UX engineer. Build a docs site with search-friendly structure for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 41.
```

### `web-dev-042` — build a SaaS landing page with pricing and FAQ

```text
Act as an expert SEO copywriter. Build a SaaS landing page with pricing and FAQ for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 42.
```

### `web-dev-043` — create a job-board page with filters and alerts copy

```text
Act as a senior frontend engineer. Create a job-board page with filters and alerts copy for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 43.
```

### `web-dev-044` — create a restaurant site with menu, reservations, and local SEO

```text
Act as an expert web designer. Create a restaurant site with menu, reservations, and local SEO for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 44.
```

### `web-dev-045` — build a nonprofit donation page with impact stories

```text
Act as a senior conversion strategist. Build a nonprofit donation page with impact stories for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 45.
```

### `web-dev-046` — build a luxury auto-detailing site with booking flow

```text
Act as a full-stack web developer. Build a luxury auto-detailing site with booking flow for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 46.
```

### `web-dev-047` — create a course landing page with curriculum and outcomes

```text
Act as a senior UX engineer. Create a course landing page with curriculum and outcomes for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 47.
```

### `web-dev-048` — run a frontend QA pass: nav, forms, mobile, a11y

```text
Act as an expert SEO copywriter. Run a frontend QA pass: nav, forms, mobile, a11y for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 48.
```

### `web-dev-049` — build a real-estate listing page with gallery and tour CTA

```text
Act as a senior frontend engineer. Build a real-estate listing page with gallery and tour CTA for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 49.
```

### `web-dev-050` — create a pricing-page A/B test plan with hypotheses

```text
Act as an expert web designer. Create a pricing-page A/B test plan with hypotheses for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 50.
```

### `web-dev-051` — create an event site with schedule, speakers, and tickets

```text
Act as a senior conversion strategist. Create an event site with schedule, speakers, and tickets for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 51.
```

### `web-dev-052` — build a waitlist page with referral mechanics

```text
Act as a full-stack web developer. Build a waitlist page with referral mechanics for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 52.
```

### `web-dev-053` — build a clinic booking site with forms and reminders copy

```text
Act as a senior UX engineer. Build a clinic booking site with forms and reminders copy for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 53.
```

### `web-dev-054` — create a changelog page with version filters

```text
Act as an expert SEO copywriter. Create a changelog page with version filters for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 54.
```

### `web-dev-055` — create a portfolio site with case-study templates

```text
Act as a senior frontend engineer. Create a portfolio site with case-study templates for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 55.
```

### `web-dev-056` — build a docs site with search-friendly structure

```text
Act as an expert web designer. Build a docs site with search-friendly structure for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 56.
```

### `web-dev-057` — build a SaaS landing page with pricing and FAQ

```text
Act as a senior conversion strategist. Build a SaaS landing page with pricing and FAQ for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 57.
```

### `web-dev-058` — create a job-board page with filters and alerts copy

```text
Act as a full-stack web developer. Create a job-board page with filters and alerts copy for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 58.
```

### `web-dev-059` — create a restaurant site with menu, reservations, and local SEO

```text
Act as a senior UX engineer. Create a restaurant site with menu, reservations, and local SEO for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 59.
```

### `web-dev-060` — build a nonprofit donation page with impact stories

```text
Act as an expert SEO copywriter. Build a nonprofit donation page with impact stories for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 60.
```

### `web-dev-061` — build a luxury auto-detailing site with booking flow

```text
Act as a senior frontend engineer. Build a luxury auto-detailing site with booking flow for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 61.
```

### `web-dev-062` — create a course landing page with curriculum and outcomes

```text
Act as an expert web designer. Create a course landing page with curriculum and outcomes for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 62.
```

### `web-dev-063` — run a frontend QA pass: nav, forms, mobile, a11y

```text
Act as a senior conversion strategist. Run a frontend QA pass: nav, forms, mobile, a11y for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 63.
```

### `web-dev-064` — build a real-estate listing page with gallery and tour CTA

```text
Act as a full-stack web developer. Build a real-estate listing page with gallery and tour CTA for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 64.
```

### `web-dev-065` — create a pricing-page A/B test plan with hypotheses

```text
Act as a senior UX engineer. Create a pricing-page A/B test plan with hypotheses for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 65.
```

### `web-dev-066` — create an event site with schedule, speakers, and tickets

```text
Act as an expert SEO copywriter. Create an event site with schedule, speakers, and tickets for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 66.
```

### `web-dev-067` — build a waitlist page with referral mechanics

```text
Act as a senior frontend engineer. Build a waitlist page with referral mechanics for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 67.
```

### `web-dev-068` — build a clinic booking site with forms and reminders copy

```text
Act as an expert web designer. Build a clinic booking site with forms and reminders copy for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 68.
```

### `web-dev-069` — create a changelog page with version filters

```text
Act as a senior conversion strategist. Create a changelog page with version filters for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 69.
```

### `web-dev-070` — create a portfolio site with case-study templates

```text
Act as a full-stack web developer. Create a portfolio site with case-study templates for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 70.
```

### `web-dev-071` — build a docs site with search-friendly structure

```text
Act as a senior UX engineer. Build a docs site with search-friendly structure for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 71.
```

### `web-dev-072` — build a SaaS landing page with pricing and FAQ

```text
Act as an expert SEO copywriter. Build a SaaS landing page with pricing and FAQ for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 72.
```

### `web-dev-073` — create a job-board page with filters and alerts copy

```text
Act as a senior frontend engineer. Create a job-board page with filters and alerts copy for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 73.
```

### `web-dev-074` — create a restaurant site with menu, reservations, and local SEO

```text
Act as an expert web designer. Create a restaurant site with menu, reservations, and local SEO for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 74.
```

### `web-dev-075` — build a nonprofit donation page with impact stories

```text
Act as a senior conversion strategist. Build a nonprofit donation page with impact stories for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 75.
```

### `web-dev-076` — build a luxury auto-detailing site with booking flow

```text
Act as a full-stack web developer. Build a luxury auto-detailing site with booking flow for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 76.
```

### `web-dev-077` — create a course landing page with curriculum and outcomes

```text
Act as a senior UX engineer. Create a course landing page with curriculum and outcomes for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 77.
```

### `web-dev-078` — run a frontend QA pass: nav, forms, mobile, a11y

```text
Act as an expert SEO copywriter. Run a frontend QA pass: nav, forms, mobile, a11y for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 78.
```

### `web-dev-079` — build a real-estate listing page with gallery and tour CTA

```text
Act as a senior frontend engineer. Build a real-estate listing page with gallery and tour CTA for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 79.
```

### `web-dev-080` — create a pricing-page A/B test plan with hypotheses

```text
Act as an expert web designer. Create a pricing-page A/B test plan with hypotheses for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 80.
```

### `web-dev-081` — create an event site with schedule, speakers, and tickets

```text
Act as a senior conversion strategist. Create an event site with schedule, speakers, and tickets for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 81.
```

### `web-dev-082` — build a waitlist page with referral mechanics

```text
Act as a full-stack web developer. Build a waitlist page with referral mechanics for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 82.
```

### `web-dev-083` — build a clinic booking site with forms and reminders copy

```text
Act as a senior UX engineer. Build a clinic booking site with forms and reminders copy for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 83.
```

### `web-dev-084` — create a changelog page with version filters

```text
Act as an expert SEO copywriter. Create a changelog page with version filters for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 84.
```

### `web-dev-085` — create a portfolio site with case-study templates

```text
Act as a senior frontend engineer. Create a portfolio site with case-study templates for a local business. The intended result is a complete page spec plus copy deck. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 85.
```

### `web-dev-086` — build a docs site with search-friendly structure

```text
Act as an expert web designer. Build a docs site with search-friendly structure for a nonprofit. The intended result is a launch checklist. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 86.
```

### `web-dev-087` — build a SaaS landing page with pricing and FAQ

```text
Act as a senior conversion strategist. Build a SaaS landing page with pricing and FAQ for a school. The intended result is a component list with states. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 87.
```

### `web-dev-088` — create a job-board page with filters and alerts copy

```text
Act as a full-stack web developer. Create a job-board page with filters and alerts copy for a clinic. The intended result is an SEO checklist with titles/metas. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 88.
```

### `web-dev-089` — create a restaurant site with menu, reservations, and local SEO

```text
Act as a senior UX engineer. Create a restaurant site with menu, reservations, and local SEO for a creator. The intended result is a sitemap with URL slugs. Requirements: mobile-responsive; strong CTAs; refined typography; no lorem ipsum; real copy throughout. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 89.
```

### `web-dev-090` — build a nonprofit donation page with impact stories

```text
Act as an expert SEO copywriter. Build a nonprofit donation page with impact stories for a SaaS startup. The intended result is a QA report with fixes applied. Requirements: fast load: note image sizes and font choices; test every CTA and form before calling it done. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 90.
```
