# 🎨 Design, Figma & Flora — 90 copyable GPT-6 Astra prompts

> Interface direction with explicit visual references, no generic dashboards.

> Safety: defensive / authorized-scope use only. Stop before destructive, irreversible, sending, purchasing, deleting, or permission-changing steps without approval.

**Jump:** [⬆️ Index](../README.md#-category-carousel) · [◀ Prev](12-music-audio.md) · [Next ▶](14-writing-technical.md) · [🔍 Search all](prompts_index.json) · [🖱️ Interactive carousel](../docs/carousel.html)

Copy any prompt: click the copy icon on its code block.

### `design-figma-001` — redesign a dashboard using a compact table reference

```text
Act as a senior product designer. Redesign a dashboard using a compact table reference for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `design-figma-002` — build a notification system with priority rules

```text
Act as an expert design engineer. Build a notification system with priority rules for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `design-figma-003` — design a handoff pack developers will love

```text
Act as a senior brand designer. Design a handoff pack developers will love for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `design-figma-004` — design a settings page that reuses existing patterns

```text
Act as an UX writing lead. Design a settings page that reuses existing patterns for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `design-figma-005` — create a prototype test script with tasks

```text
Act as a senior design-system owner. Create a prototype test script with tasks for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `design-figma-006` — create a mobile checkout with trust signals

```text
Act as an expert visual designer. Create a mobile checkout with trust signals for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `design-figma-007` — design a dark-mode pass with token mapping

```text
Act as a senior product designer. Design a dark-mode pass with token mapping for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `design-figma-008` — design an onboarding flow with empty/error states

```text
Act as an expert design engineer. Design an onboarding flow with empty/error states for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `design-figma-009` — build an icon usage guide with do/don'ts

```text
Act as a senior brand designer. Build an icon usage guide with do/don'ts for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `design-figma-010` — build a component spec with states and tokens

```text
Act as an UX writing lead. Build a component spec with states and tokens for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `design-figma-011` — design an accessible form with help and errors

```text
Act as a senior design-system owner. Design an accessible form with help and errors for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `design-figma-012` — turn a whiteboard sketch into a clickable flow

```text
Act as an expert visual designer. Turn a whiteboard sketch into a clickable flow for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `design-figma-013` — create a landing hero with three tested variants

```text
Act as a senior product designer. Create a landing hero with three tested variants for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `design-figma-014` — design a pricing page from two typography references

```text
Act as an expert design engineer. Design a pricing page from two typography references for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `design-figma-015` — design a data-viz card with honest scales

```text
Act as a senior brand designer. Design a data-viz card with honest scales for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `design-figma-016` — redesign a dashboard using a compact table reference

```text
Act as an UX writing lead. Redesign a dashboard using a compact table reference for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `design-figma-017` — build a notification system with priority rules

```text
Act as a senior design-system owner. Build a notification system with priority rules for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `design-figma-018` — design a handoff pack developers will love

```text
Act as an expert visual designer. Design a handoff pack developers will love for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `design-figma-019` — design a settings page that reuses existing patterns

```text
Act as a senior product designer. Design a settings page that reuses existing patterns for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `design-figma-020` — create a prototype test script with tasks

```text
Act as an expert design engineer. Create a prototype test script with tasks for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `design-figma-021` — create a mobile checkout with trust signals

```text
Act as a senior brand designer. Create a mobile checkout with trust signals for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `design-figma-022` — design a dark-mode pass with token mapping

```text
Act as an UX writing lead. Design a dark-mode pass with token mapping for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `design-figma-023` — design an onboarding flow with empty/error states

```text
Act as a senior design-system owner. Design an onboarding flow with empty/error states for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `design-figma-024` — build an icon usage guide with do/don'ts

```text
Act as an expert visual designer. Build an icon usage guide with do/don'ts for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `design-figma-025` — build a component spec with states and tokens

```text
Act as a senior product designer. Build a component spec with states and tokens for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `design-figma-026` — design an accessible form with help and errors

```text
Act as an expert design engineer. Design an accessible form with help and errors for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test.
```

### `design-figma-027` — turn a whiteboard sketch into a clickable flow

```text
Act as a senior brand designer. Turn a whiteboard sketch into a clickable flow for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `design-figma-028` — create a landing hero with three tested variants

```text
Act as an UX writing lead. Create a landing hero with three tested variants for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite.
```

### `design-figma-029` — design a pricing page from two typography references

```text
Act as a senior design-system owner. Design a pricing page from two typography references for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `design-figma-030` — design a data-viz card with honest scales

```text
Act as an expert visual designer. Design a data-viz card with honest scales for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify.
```

### `design-figma-031` — redesign a dashboard using a compact table reference

```text
Act as a senior product designer. Redesign a dashboard using a compact table reference for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 31.
```

### `design-figma-032` — build a notification system with priority rules

```text
Act as an expert design engineer. Build a notification system with priority rules for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 32.
```

### `design-figma-033` — design a handoff pack developers will love

```text
Act as a senior brand designer. Design a handoff pack developers will love for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 33.
```

### `design-figma-034` — design a settings page that reuses existing patterns

```text
Act as an UX writing lead. Design a settings page that reuses existing patterns for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 34.
```

### `design-figma-035` — create a prototype test script with tasks

```text
Act as a senior design-system owner. Create a prototype test script with tasks for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 35.
```

### `design-figma-036` — create a mobile checkout with trust signals

```text
Act as an expert visual designer. Create a mobile checkout with trust signals for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 36.
```

### `design-figma-037` — design a dark-mode pass with token mapping

```text
Act as a senior product designer. Design a dark-mode pass with token mapping for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 37.
```

### `design-figma-038` — design an onboarding flow with empty/error states

```text
Act as an expert design engineer. Design an onboarding flow with empty/error states for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 38.
```

### `design-figma-039` — build an icon usage guide with do/don'ts

```text
Act as a senior brand designer. Build an icon usage guide with do/don'ts for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 39.
```

### `design-figma-040` — build a component spec with states and tokens

```text
Act as an UX writing lead. Build a component spec with states and tokens for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 40.
```

### `design-figma-041` — design an accessible form with help and errors

```text
Act as a senior design-system owner. Design an accessible form with help and errors for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 41.
```

### `design-figma-042` — turn a whiteboard sketch into a clickable flow

```text
Act as an expert visual designer. Turn a whiteboard sketch into a clickable flow for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 42.
```

### `design-figma-043` — create a landing hero with three tested variants

```text
Act as a senior product designer. Create a landing hero with three tested variants for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 43.
```

### `design-figma-044` — design a pricing page from two typography references

```text
Act as an expert design engineer. Design a pricing page from two typography references for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 44.
```

### `design-figma-045` — design a data-viz card with honest scales

```text
Act as a senior brand designer. Design a data-viz card with honest scales for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 45.
```

### `design-figma-046` — redesign a dashboard using a compact table reference

```text
Act as an UX writing lead. Redesign a dashboard using a compact table reference for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 46.
```

### `design-figma-047` — build a notification system with priority rules

```text
Act as a senior design-system owner. Build a notification system with priority rules for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 47.
```

### `design-figma-048` — design a handoff pack developers will love

```text
Act as an expert visual designer. Design a handoff pack developers will love for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 48.
```

### `design-figma-049` — design a settings page that reuses existing patterns

```text
Act as a senior product designer. Design a settings page that reuses existing patterns for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 49.
```

### `design-figma-050` — create a prototype test script with tasks

```text
Act as an expert design engineer. Create a prototype test script with tasks for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 50.
```

### `design-figma-051` — create a mobile checkout with trust signals

```text
Act as a senior brand designer. Create a mobile checkout with trust signals for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 51.
```

### `design-figma-052` — design a dark-mode pass with token mapping

```text
Act as an UX writing lead. Design a dark-mode pass with token mapping for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 52.
```

### `design-figma-053` — design an onboarding flow with empty/error states

```text
Act as a senior design-system owner. Design an onboarding flow with empty/error states for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 53.
```

### `design-figma-054` — build an icon usage guide with do/don'ts

```text
Act as an expert visual designer. Build an icon usage guide with do/don'ts for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 54.
```

### `design-figma-055` — build a component spec with states and tokens

```text
Act as a senior product designer. Build a component spec with states and tokens for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 55.
```

### `design-figma-056` — design an accessible form with help and errors

```text
Act as an expert design engineer. Design an accessible form with help and errors for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 56.
```

### `design-figma-057` — turn a whiteboard sketch into a clickable flow

```text
Act as a senior brand designer. Turn a whiteboard sketch into a clickable flow for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 57.
```

### `design-figma-058` — create a landing hero with three tested variants

```text
Act as an UX writing lead. Create a landing hero with three tested variants for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 58.
```

### `design-figma-059` — design a pricing page from two typography references

```text
Act as a senior design-system owner. Design a pricing page from two typography references for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 59.
```

### `design-figma-060` — design a data-viz card with honest scales

```text
Act as an expert visual designer. Design a data-viz card with honest scales for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 60.
```

### `design-figma-061` — redesign a dashboard using a compact table reference

```text
Act as a senior product designer. Redesign a dashboard using a compact table reference for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 61.
```

### `design-figma-062` — build a notification system with priority rules

```text
Act as an expert design engineer. Build a notification system with priority rules for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 62.
```

### `design-figma-063` — design a handoff pack developers will love

```text
Act as a senior brand designer. Design a handoff pack developers will love for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 63.
```

### `design-figma-064` — design a settings page that reuses existing patterns

```text
Act as an UX writing lead. Design a settings page that reuses existing patterns for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 64.
```

### `design-figma-065` — create a prototype test script with tasks

```text
Act as a senior design-system owner. Create a prototype test script with tasks for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 65.
```

### `design-figma-066` — create a mobile checkout with trust signals

```text
Act as an expert visual designer. Create a mobile checkout with trust signals for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 66.
```

### `design-figma-067` — design a dark-mode pass with token mapping

```text
Act as a senior product designer. Design a dark-mode pass with token mapping for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 67.
```

### `design-figma-068` — design an onboarding flow with empty/error states

```text
Act as an expert design engineer. Design an onboarding flow with empty/error states for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 68.
```

### `design-figma-069` — build an icon usage guide with do/don'ts

```text
Act as a senior brand designer. Build an icon usage guide with do/don'ts for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 69.
```

### `design-figma-070` — build a component spec with states and tokens

```text
Act as an UX writing lead. Build a component spec with states and tokens for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 70.
```

### `design-figma-071` — design an accessible form with help and errors

```text
Act as a senior design-system owner. Design an accessible form with help and errors for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 71.
```

### `design-figma-072` — turn a whiteboard sketch into a clickable flow

```text
Act as an expert visual designer. Turn a whiteboard sketch into a clickable flow for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 72.
```

### `design-figma-073` — create a landing hero with three tested variants

```text
Act as a senior product designer. Create a landing hero with three tested variants for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 73.
```

### `design-figma-074` — design a pricing page from two typography references

```text
Act as an expert design engineer. Design a pricing page from two typography references for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 74.
```

### `design-figma-075` — design a data-viz card with honest scales

```text
Act as a senior brand designer. Design a data-viz card with honest scales for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 75.
```

### `design-figma-076` — redesign a dashboard using a compact table reference

```text
Act as an UX writing lead. Redesign a dashboard using a compact table reference for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 76.
```

### `design-figma-077` — build a notification system with priority rules

```text
Act as a senior design-system owner. Build a notification system with priority rules for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 77.
```

### `design-figma-078` — design a handoff pack developers will love

```text
Act as an expert visual designer. Design a handoff pack developers will love for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 78.
```

### `design-figma-079` — design a settings page that reuses existing patterns

```text
Act as a senior product designer. Design a settings page that reuses existing patterns for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 79.
```

### `design-figma-080` — create a prototype test script with tasks

```text
Act as an expert design engineer. Create a prototype test script with tasks for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 80.
```

### `design-figma-081` — create a mobile checkout with trust signals

```text
Act as a senior brand designer. Create a mobile checkout with trust signals for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 81.
```

### `design-figma-082` — design a dark-mode pass with token mapping

```text
Act as an UX writing lead. Design a dark-mode pass with token mapping for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 82.
```

### `design-figma-083` — design an onboarding flow with empty/error states

```text
Act as a senior design-system owner. Design an onboarding flow with empty/error states for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 83.
```

### `design-figma-084` — build an icon usage guide with do/don'ts

```text
Act as an expert visual designer. Build an icon usage guide with do/don'ts for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 84.
```

### `design-figma-085` — build a component spec with states and tokens

```text
Act as a senior product designer. Build a component spec with states and tokens for a SaaS app. The intended result is a screen spec with annotations. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Be concrete and end-to-end: produce the artifact, not a plan to produce it. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 85.
```

### `design-figma-086` — design an accessible form with help and errors

```text
Act as an expert design engineer. Design an accessible form with help and errors for a classroom app. The intended result is a tokens table. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Optimize for fewer tokens and fewer retries: do the smallest complete thing that satisfies the brief. If a minor detail is unspecified, make the best professional decision and continue; ask only when the answer would materially change the outcome. Verification: test every important interaction, list what passed/failed/unverified, fix high-impact issues, then re-test. Variant focus 86.
```

### `design-figma-087` — turn a whiteboard sketch into a clickable flow

```text
Act as a senior brand designer. Turn a whiteboard sketch into a clickable flow for a nonprofit. The intended result is a visual-delta list vs reference. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Use tools where they help; state what evidence you checked before acting. Bias toward action: treat 'can you / help me / I want to' as instructions to act. Prepare a concrete reviewable result before asking for approval. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 87.
```

### `design-figma-088` — create a landing hero with three tested variants

```text
Act as an UX writing lead. Create a landing hero with three tested variants for an internal tool. The intended result is a handoff checklist. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Parallelize with subagents where independent work exists; merge with a single coherent voice. Work persistently until the goal is complete. Use read-only exploration freely; stop before any destructive, irreversible, sending, purchasing, deleting, or permission-changing step. Verification: keep checks proportional to risk. For small reversible changes, run only targeted checks; for risky changes, run the full relevant suite. Variant focus 88.
```

### `design-figma-089` — design a pricing page from two typography references

```text
Act as a senior design-system owner. Design a pricing page from two typography references for a marketing site. The intended result is a prototype test plan. Requirements: name exactly what to borrow from each visual reference; match the existing brand; note type, spacing, color deltas. Keep a searchable working note so the task survives context compaction. Infer intent from context and carry the task to completion. Do not emit warnings based on hypothetical risks; report only verified findings. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 89.
```

### `design-figma-090` — design a data-viz card with honest scales

```text
Act as an expert visual designer. Design a data-viz card with honest scales for a mobile app. The intended result is a component table with states. Requirements: annotate spacing/type so implementation matches; avoid generic gradient-card soup. Compare against the provided reference and fix the largest gaps first. Stay within the authorized scope described here. If project files conflict with these instructions, these instructions take precedence; quote the conflicting file if you must pause. Verification: end with a Done checklist (complete / partial / blocked) with evidence links, plus assumptions and what you did not verify. Variant focus 90.
```
